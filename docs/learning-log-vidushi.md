| Date | Task ID | What it does | Why this approach | Question I'd get asked |
|---|---|---|---|---|
| 2026-07-19 | P0 | Designed the Prisma schema in `packages/db/prisma/schema.prisma` with 12 models covering users, authentication, repositories, pull requests, analysis jobs, health snapshots, findings, quality gates, notifications, and mobile devices | Keeping the schema in code makes database changes version-controlled and reviewable, while Prisma generates a type-safe client for the TypeScript applications | Why use Prisma instead of writing SQL directly? |
| 2026-07-19 | P0 | Added 12 enums for roles, statuses, severity, finding categories, notification types, and mobile platforms | PostgreSQL enums restrict these fields to known values and prevent invalid application data from being stored | What is the difference between `Severity` and `FindingCategory`? |
| 2026-07-19 | P0 | Defined model relations, foreign keys, unique constraints, and delete behavior such as `Cascade`, `Restrict`, and `SetNull` | Explicit constraints preserve data integrity and clearly define what happens to dependent records when parent records change or are deleted | Why do different relations use different `onDelete` behaviors? |
| 2026-07-19 | P0 | Added composite indexes for common dashboard queries, including repository status, notification read state, and health snapshots ordered by calculation time | These indexes support the fields used together for filtering and sorting, reducing expensive table scans as data grows | Why use a composite index instead of separate indexes for every field? |
| 2026-07-19 | P0 | Designed secure credential fields: GitHub access tokens are stored encrypted and session tokens are stored as hashes rather than raw values | An encrypted token can only be recovered with the encryption key, while a one-way token hash allows session verification without storing the original session token | Why encrypt an access token but hash a session token? |
| 2026-07-19 | P0 | Ran `npx prisma migrate dev --name init`, generating the initial `migration.sql` and applying it to the local PostgreSQL `code_review_db` database | `migrate dev` creates and applies migrations during development; production should use `migrate deploy` to apply reviewed migration files | What is the difference between `prisma migrate dev` and `prisma migrate deploy`? |
| 2026-07-19 | P0 | Added `prisma.config.ts`, the migration lock file, and `.env.example`, while keeping the real `.env` database credentials out of Git | The configuration gives Prisma a consistent schema and migration location, and `.env.example` documents setup without exposing secrets | Why commit `.env.example` but not `.env`? |
| 2026-07-19 | P0 | Committed the database package and migration files on the `devops-vidushi` feature branch and submitted the work through pull request #8 | A feature branch and pull request keep incomplete work isolated and allow review before it reaches the shared `develop` branch | Why should migration files be committed to Git? |
| 2026-07-25 | P0 | Created `packages/db/prisma/seed.js` with linked demo records for 10 models: users, repository membership, pull request, analysis job, health snapshot, findings, quality gate, notification, and mobile device | Fixed `seed-*` identifiers and Prisma `upsert` operations make the seed predictable and safe to run repeatedly; only seed-owned findings and notifications are replaced | Why use fixed values and `upsert` instead of generating completely random records? |
| 2026-07-25 | P0 | Deliberately left `GitHubCredential` and `Session` out of the shared seed | These authentication tables hold encrypted access tokens and token hashes; fake records would test relationships but would not provide valid GitHub authentication and could be mistaken for usable credentials | Why does the seed not insert data into every table? |
| 2026-07-25 | P0 | Added `db:generate`, `db:migrate`, `db:seed`, and `db:validate` package scripts and registered the seed command in `prisma.config.ts` | Named scripts give every teammate the same short, documented commands for common Prisma operations | How does Prisma know which seed file to execute? |
| 2026-07-25 | P0 | Replaced the incompatible TypeScript 7/`ts-node` seed runner with Node.js and configured Prisma 7 with `@prisma/adapter-pg` and `pg` | Prisma 7 requires an explicit PostgreSQL driver adapter, and plain JavaScript avoids the `ts-node` compiler incompatibility without changing the database logic | Why does Prisma 7 need a PostgreSQL adapter passed to `PrismaClient`? |
| 2026-07-25 | P0 | Ran the seed repeatedly and validated the result in local PostgreSQL: 2 users, 1 repository, 1 snapshot, 3 findings, and 1 notification | A second successful run with unchanged counts verifies that the seed is idempotent and does not accumulate duplicate demo data | How did you verify that rerunning the seed does not create duplicates? |
| 2026-07-27 | C-05 | Bulk-inserted throwaway data (4 extra repos, ~1,000 snapshots, ~4,000 findings, 2,000 "noise" repos) before running `EXPLAIN ANALYZE`, then deleted it all and confirmed row counts matched the C-04 seed exactly | The C-04 seed only has a handful of rows per table; Postgres correctly avoids using an index on a 3-row table regardless of how good the index is, so validating against it would have shown Seq Scans everywhere and proven nothing | Why not just run `EXPLAIN ANALYZE` against the existing seed data? |
| 2026-07-27 | C-05 | Measured the trend query (`HealthSnapshot` by repo + date range) and both findings queries against real data and recorded the actual plans in `database_design.md`, replacing the old reasoning-only "Verified ✅" notes | The original notes were justified from the index definitions on paper, not from a real query plan; C-05's whole point is to check that reasoning against what Postgres actually does | What's the difference between saying an index "should" be used and proving it "is" used? |
| 2026-07-27 | C-05 | Found that the findings state/severity breakdown query doesn't actually use `[snapshotId, state]` — it uses `[snapshotId, file]` instead, because both indexes share `snapshotId` as the leading column and the query has no filter beyond that | Composite indexes are only "needed" for their second column when a query filters on it; an index whose second column is never a WHERE condition here (state/severity are only in GROUP BY) doesn't get chosen for that reason, and any sibling index sharing the same leading column would do just as well | If `[snapshotId, state]` isn't being used here, does that mean it's dead weight? |
| 2026-07-27 | C-05 | Constructed the `GET /api/repos` list query from the `api_design.md` contract (it isn't implemented yet) and added it to `database_design.md` as Query 7 so it has a documented, index-checked shape before someone builds the route | C-05 asked me to validate a "repo list" query, but no such query existed yet in the docs or the codebase to validate — writing it down now means the route implementer starts from a checked query instead of inventing one from scratch | Isn't writing a query for an unbuilt route getting ahead of the actual implementation task? |
| 2026-07-27 | C-05 | Padded `Repository` to 2,005 rows specifically to test the repo-list query's owner/member filter, and found Postgres chooses a Seq Scan over the `ownerId` index even at that size — documented this as expected planner behavior, not a bug | A `Seq Scan` over an indexed column looks like a red flag at first glance, but `OR EXISTS(...)` can't collapse into one index condition, and scanning 2,000 rows (54 pages) is genuinely cheaper than two index scans plus a bitmap merge; the query still ran in under 1ms | How do you tell a real missing-index problem apart from the planner correctly picking a sequential scan? |
| 2026-07-29 | A-13 | Implemented `GET /api/repos/:repoId/trend` — `repoService.ts` (repo existence check, query parsing, the Prisma query) plus `routes/repos.ts` and mounting it in `app.ts` — as the first `/api/repos/*` endpoint in the codebase, since no repo router or repo-scoped auth check existed yet | Following the existing `auth.ts`/`webhookService.ts` pattern (thin route → service function → `AppError` on failure) kept the endpoint consistent with the rest of the API instead of inventing a new structure for the first repo route | Why does this route only check that the repo exists, not that the caller owns or is a member of it? |
| 2026-07-29 | A-13 | Made `from`/`to` win over `days` when a request sends both, and treat a request with only one of `from`/`to` as a `400`, since `api_design.md` doesn't say what happens when both params are present | An explicit date range is a more deliberate signal than the `days` shorthand, so the more specific input should win; a half-supplied range is a client mistake, not a valid shorthand, so it should fail loudly instead of silently falling back to the 30-day default | What would happen if a client sent `?days=7&from=2026-01-01&to=2026-01-31` before this decision was made? |
| 2026-07-29 | A-13 | Queried `HealthSnapshot` with `calculatedAt: { gte: from, lte: to }` instead of `gt`/`lt` | A snapshot calculated at exactly the `from` or `to` boundary is still inside the requested range from the caller's point of view; `gt`/`lt` would silently drop it, an off-by-one bug at the edge of the range | Why would using `gt` instead of `gte` here be a bug and not just a stylistic choice? |
| 2026-07-29 | A-13 | Verified all four cases (`200`/`401`/`404`/`400`) against real local Postgres by hand-signing a JWT with `jsonwebtoken` using the same `JWT_SECRET` the running server had loaded, instead of going through `/auth/github` | The GitHub OAuth login flow needs a real GitHub App and can't be exercised locally without one; since `requireAuth` only verifies the JWT signature and reads `sub`/`username`/`platformRole` out of it (no DB session lookup), a correctly-signed token is indistinguishable from one issued by a real login for testing purposes | Why is it safe to test the endpoint with a hand-crafted token instead of a real login? |
| 2026-07-30 | A-14 | Implemented `GET /api/repos/:repoId/debt` — added `getRepoDebt` to `repoService.ts` and wired the route in `routes/repos.ts`, following the same thin route → service function → `AppError` pattern as `/trend` | Keeps the second `/api/repos/*` endpoint consistent with the first instead of introducing a new structure two endpoints in | Why does this endpoint reuse `getActiveRepo` instead of writing its own repo-existence check? |
| 2026-07-30 | A-14 | Scoped the route with `requireAuth` + `requireRepoAccess('read')` rather than matching `/trend`'s auth-only check, even though the spec lists "Auth: Required" identically for both | Debt figures are closer to `/members`-level sensitivity than a public trend line, and the spec being silent on membership isn't the same as the spec ruling it out; flagged the inconsistency with `/trend` rather than silently copying it | Should `/trend` be tightened to match, given it has the same gap? |
| 2026-07-30 | A-14 | Return `404 NOT_FOUND` when a repo has zero `HealthSnapshot` rows, instead of a `200` with an all-zero breakdown | Neither `api_design.md` nor `database_design.md` specifies this case; treating "never analyzed" as an error keeps it distinguishable from "analyzed and found zero debt," which a zeroed `200` would hide | How would a caller tell "no analysis yet" apart from "genuinely zero debt" if this returned `200`? |
| 2026-07-30 | A-14 | Aggregated `Finding` rows with `groupBy({ by: ['category'], where: { snapshotId } })`, filtering on `snapshotId` rather than `repoId`, then zero-filled all 5 categories before overlaying the grouped results | Debt breakdown is a snapshot-in-time view; filtering on `repoId` alone would sum findings across every historical snapshot for that repo and overcount. Zero-filling first guarantees categories with no findings (e.g. `maintainability`) still appear, matching the `api_design.md` example response | What would the response look like for a category with no findings if the zero-fill step were skipped? |
| 2026-07-30 | A-14 | Mapped the Prisma `FindingCategory` enum to the response's lowercase snake_case keys with an explicit `Record` lookup instead of `.toLowerCase()`, even though both produce the same string today | An explicit map is the honest source of truth for which 5 categories exist in the API contract; a new enum value added later would be silently included by `.toLowerCase()` with a guessed key, whereas the explicit map would leave it out until someone updates it on purpose | Since `.toLowerCase()` produces identical output today, why not use it and save a few lines? |
| 2026-07-30 | A-14 | Verified all six cases (`401`, `403` non-member, `200` owner, `200` member, `404` unknown repo, `404` zero-snapshot repo) against real seed data, cross-checking the `200` response numbers against the raw `HealthSnapshot`/`Finding` rows rather than just checking the status code | A `200` with the wrong numbers is a worse bug than a wrong status code and wouldn't be caught by status-only testing; the zero-snapshot case needed a second, temporary repo row created and deleted specifically for that test since the seed only ever creates one analyzed repo | Why wasn't checking for `200 OK` enough to call the aggregation correct? |
| 2026-08-03 | E-01 | Scaffolded `apps/mobile` with `create-expo-app` (blank TypeScript template) on Expo SDK 57, added `react-native-web`/`react-dom` for browser preview, and confirmed npm workspaces picks it up cleanly from the repo root with no duplicate `node_modules` | `project_proposal.md` names Expo v51, but that line predates the current SDK by several major versions and isn't one of the locked stack decisions in `docs/CLAUDE.md` — SDK 57 is the actively supported version, so scaffolding onto an already-outdated SDK would just create an upgrade task later for no benefit now | Why scaffold on SDK 57 instead of the SDK 51 named in the proposal doc? |
| 2026-08-03 | E-01 | Deleted `LICENSE`, `AGENTS.md`, `CLAUDE.md`, and `.claude/settings.json` that `create-expo-app` had generated inside `apps/mobile` before committing | The template ships its own MIT license file (Expo's copyright, not this project's) and a `.claude/settings.json` that silently enables an unrelated Claude Code plugin; neither is something this task asked for, and committing them would be an unreviewed tooling change riding along with the scaffold | What would have happened if the `.claude/settings.json` file had been committed as-is? |
| 2026-08-03 | E-01 | Verified the scaffold by running `npx expo start` and opening the app in-browser (`w`), rather than trusting a clean `npm install` alone as proof it worked | A dependency install finishing without errors only proves the packages resolved, not that the app actually boots and renders; Metro bundling and a rendered "Open up App.tsx" screen is the actual acceptance criterion | Why wasn't a successful `npm install` enough to call the scaffold done? |
| 2026-08-03 | E-02 | Added `@react-navigation/native` + `@react-navigation/bottom-tabs` (with `react-native-screens`/`react-native-safe-area-context` peer deps) instead of switching the project to Expo Router | `apps/mobile` still uses the plain `App.tsx`/`index.ts` entry from E-01; Expo Router needs a file-based `app/` directory restructure, a bigger change than a scaffolding step justifies, whereas React Navigation drops into the existing entry point with explicit, code-visible routing | Why not use Expo Router since it ships with newer Expo templates? |
| 2026-08-03 | E-02 | Scaffolded only two tabs — Home and Notifications — in `navigation/TabNavigator.tsx`, not a third Profile tab | Those are the only two mobile screens with an actual WBS task behind them (`E-05` home, `E-07` notifications); a Profile tab has no assigned task yet, so adding it now would be a speculative screen nobody asked for | Why not add a Profile tab now to avoid restructuring the navigator later? |
| 2026-08-03 | E-02 | Created `screens/HomeScreen.tsx` and `screens/NotificationsScreen.tsx` as empty, identically-shaped placeholders (centered `Text` in a `View`), and replaced `App.tsx`'s entire placeholder content with `<TabNavigator />` | This step's acceptance criteria is scaffolding only, no real data; keeping both screens structurally identical signals to a reviewer they're matching stubs, and the old `App.tsx` "Open up App.tsx" content was fully superseded once the navigator became the app's root | Why replace all of `App.tsx` instead of keeping its old content alongside the navigator? |
| 2026-08-03 | E-02 | Verified by running the app with `npm run web --workspace=apps/mobile` and confirming the bottom tab bar renders both tabs and switches screens on tap, not just that the TypeScript compiled | A clean compile only proves the navigation types line up; the actual acceptance criterion is that `NavigationContainer`/`createBottomTabNavigator` actually mount and respond to taps at runtime | What could compile cleanly here but still fail to render a working tab bar? |
| 2026-08-05 | A-15 | Implemented `GET /api/repos/:repoId/hotspots` — added `getRepoHotspots` to `repoService.ts` and wired the route in `routes/repos.ts` with the same `requireAuth` + `requireRepoAccess('read')` guard and thin route → service pattern as `/debt` and `/trend` | Ranks files by aggregating `Finding` with `groupBy(['file'])` ordered by `_count` desc, then runs a second `groupBy(['file','severity','state'])` scoped to only the top-ranked files for the per-severity/new-count breakdown, reusing the `[snapshotId, file]` index that `database_design.md` had already reserved for this query but that nothing queried until now | Why run two separate `groupBy` queries instead of one? |
| 2026-08-05 | A-15 | Excluded `Finding` rows with `file: null` from the ranking entirely, capped `limit` at 100 (default 10, `400` outside that range), and treated a `snapshotId` query param belonging to a different repo as `404` rather than leaking that the snapshot exists elsewhere | `api_design.md` specifies the response shape but leaves these three edges undefined; a null file can't be attributed to a "worst file", an uncapped `limit` would let a client force an unbounded scan, and a distinguishable error for a cross-repo snapshot ID would be a tenant-isolation leak matching the same concern `requireRepoAccess` already guards against elsewhere in this router | What would a client be able to infer about another repo's data if the cross-repo `snapshotId` case returned a different error than "not found"? |
| 2026-08-05 | A-15 | Confirmed with `tsc --noEmit` only — have not yet exercised the endpoint against real seed data or checked the four response cases (`200` ranked correctly, `limit` clamping, cross-repo `snapshotId` `404`, zero-snapshot repo `404`) the way `/debt` and `/trend` were verified | Type-checking proves the Prisma query shapes compile against the schema, not that the ranking or zero-filled `bySeverity` output is actually correct against live data; this is a known gap, not a finished verification pass | What could type-check cleanly here but still return the wrong ranking or wrong `bySeverity` counts at runtime? |
| 2026-08-06 | A-35 | Added `lib/oauthStateStore.ts` (`consumeStateNonce`, a Redis `SET key val EX 600 NX`) and called it from `handleGithubCallback` right after `verifyState` succeeds, rejecting a second use of the same `state` with `400 INVALID_STATE` | The existing `state` was a signed JWT with a random `nonce`, but `verifyState` only checked the signature and expiry — nothing recorded that a nonce had been used, so a captured `state` value could be replayed for its whole 10-minute window; `SET NX` is atomic, so two requests racing on the same stolen value can't both pass | Why does the nonce need to be marked used in Redis instead of just checking the JWT signature and expiry? |
| 2026-08-06 | A-35 | Left the Redis call unguarded by a try/catch, so a Redis outage makes login fail with a `500` instead of silently letting logins through | The API already treats Redis as required infrastructure for the analysis queue; if this call failed open instead, the one time Redis is down the replay check would silently disable itself, which is a worse failure mode than login being briefly unavailable | Why not catch the Redis error and let login continue if Redis is unreachable? |
| 2026-08-06 | A-35 | Verified `consumeStateNonce` against real local Redis with a throwaway script calling it twice with the same string (`true` then `false`), then deleted the script; did not run the change through a real GitHub OAuth login | The isolated call proves the atomic replay check itself works against the real Redis instance; a full browser login was judged unnecessary since the change only adds one check after the existing `verifyState` call, is fully typed (`tsc --noEmit` passed), and would fail loudly on the very next real login if the wiring were wrong | What would happen on the next real login if `statePayload.nonce` were undefined for some reason? |
| 2026-08-06 | A-16 | Implemented `GET /api/repos/:repoId/pulls/:prNumber` — added `getRepoPullRequestDetail` to `repoService.ts` and wired the route in `routes/repos.ts`, same `requireAuth` + `requireRepoAccess('read')` guard and thin route → service pattern as `/debt`, `/hotspots`, and `/trend` | The `/pulls` list half of A-16 already existed (written earlier for the dashboard integration pass); only the single-PR detail endpoint was missing, so this added just that piece rather than rebuilding both | Why does this endpoint fetch every `AnalysisJob`/`HealthSnapshot` for the PR instead of just the latest one like the list endpoint does? |
| 2026-08-06 | A-16 | Left the existing `/pulls` list endpoint's response shape untouched even though it diverges from `api_design.md` (`id/author/branch/score` instead of `prNumber/authorLogin/headBranch/latestScore`, no pagination); documented the drift as a flag in `api_design.md` next to the spec instead of silently fixing it | Renaming an already-shipped endpoint's fields is a breaking change for whatever already calls it (`apps/web`), and isn't part of what A-16 was actually missing; a written flag next to the spec keeps the mismatch visible for whoever picks it up without bundling an unrelated risky change into this task | What could break if the list endpoint's field names were renamed to match the doc without checking `apps/web` first? |
| 2026-08-06 | A-16 | Added `Number.isInteger(prNumber)` validation in the route before calling the service, after discovering `GET /pulls/abc` returned a raw `500 INTERNAL_ERROR` instead of a clean `400` | `Number("abc")` is `NaN`, and passing `NaN` into `prisma.pullRequest.findUnique`'s `Int` filter throws a Prisma error rather than returning "not found," so it fell through to the generic error handler instead of a proper 4xx; validating in the route catches this before it reaches Prisma | Why does an invalid `prNumber` need explicit validation instead of letting Prisma's `findUnique` return `null` for it like a nonexistent-but-valid PR number does? |
| 2026-08-06 | A-16 | Verified all four cases against real seed data: `200` with correct `snapshots`/`newIssues` for a real PR, `404` for an unknown `prNumber`, `400` for a non-numeric `prNumber`, and `404` (repo not found, not PR data) for `seed-outsider` — a real `globex`-org user with no access to the `acme`-owned test repo — using a hand-signed JWT rather than the `demo-token` shortcut | The `demo-token` user is actually an `OWNER` of the seeded `acme` org, so testing only with it would never have exercised `requireRepoAccess`'s rejection path; `seed-outsider` exists in the seed specifically to prove cross-tenant isolation, so it was the only way to confirm a non-member is genuinely blocked rather than assumed blocked | Why wasn't testing with `demo-token` alone sufficient to call access control verified? |
| 48 | 2026-08-06 | A-17 | Implemented `GET /api/snapshots/:snapshotId/findings` — new `snapshotService.ts` and `routes/snapshots.ts`, mounted in `app.ts`, instead of adding it to `repoService.ts`/`repos.ts` | This route is keyed by `snapshotId`, not `repoId`, so it doesn't belong under the existing repo router; giving it its own service/route file matches how `notifications`/`qualityGates` each already got a dedicated file rather than being folded into an unrelated one | Why not just add one more function to the existing `repoService.ts`? |
| 49 | 2026-08-06 | A-17 | Since there's no `repoId` route param here, `requireRepoAccess` middleware can't be reused as-is; copied its org/role check inline into `snapshotService.ts` (keyed off the snapshot's own `repoId`) instead of extracting that logic out of `requireRepoAccess.ts` into a shared function | The extraction would touch a shared middleware file for a one-off task, and the duplicated block is ~15 fixed lines unlikely to drift; a third call site needing the same check would be the actual trigger to extract it, not a second one | What happens if `requireRepoAccess`'s rule set changes later — does this copy silently go stale? |
| 50 | 2026-08-06 | A-17 | Found and fixed a bug before shipping: computing `newCount` as `{...where, state: 'NEW'}` when `where` already had `state: 'EXISTING'` baked in from an `isNew=false` filter — the later key silently overwrote the filter, so a live test returned `"new": 2, "carryOver": -1` on a 1-row filtered result | Split the query into `summaryWhere` (category/severity/file only) and `dataWhere` (`summaryWhere` plus the `isNew`-derived `state`), so `summary` always reflects a stable new/carryOver breakdown independent of which `isNew` slice `data`/`pagination` are currently showing | Why should toggling `isNew` change which rows come back in `data` but not change the `summary` counts? |
| 51 | 2026-08-06 | A-17 | Verified `200` (unfiltered + each filter), `400` (bad `severity`), `401` (no token), and `404` (unknown snapshot, and `seed-outsider` hitting a snapshot in an org they're not in) against real seed data; did not exercise the `403` branch (org member with no repo access) since no seed user matches that shape | The `403` path is the same inline copy of `requireRepoAccess`'s already-tested role logic used in `/pulls/:prNumber` and friends, so it's covered by that precedent rather than by fabricating a seed row just to force a status code | Would it be worth adding a seed user in that exact "org member, no repo access" shape so this branch stops being untested? |
| 2026-08-08 | E-03 | Built the mobile login flow in `AuthContext.tsx` — `WebBrowser.openAuthSessionAsync` opens `GET /auth/github?redirect=codehealth://auth`, waits for the browser to hit that scheme, reads `token` off the returned URL, validates it with `GET /auth/me`, then persists it with `SecureStore.setItemAsync`; `App.tsx` gates the tab navigator behind `isAuthenticated` and shows `LoginScreen.tsx` otherwise | GitHub OAuth's registered `redirect_uri` is a fixed HTTPS URL on the API, not something the app can register per-client, so the app can't get a direct GitHub redirect back to itself; going through the system browser with a scheme the API can redirect to afterward avoids putting the OAuth client secret in the mobile bundle, which a token-exchange-in-app approach would have required | Why does the app open a system browser instead of exchanging the code for a token itself? |
| 2026-08-08 | E-03 | Registered `"scheme": "codehealth"` in `app.json` and added `expo-secure-store`, `expo-web-browser`, `expo-linking` via `npx expo install` rather than hand-picking versions | `expo install` resolves each package against the exact SDK 57 version already locked for this project, and it auto-registered the `expo-secure-store`/`expo-web-browser` config plugins in `app.json`, which a manual `npm install` would have left for me to add by hand and risk getting wrong | What could go wrong from installing these with plain `npm install` instead of `expo install`? |
| 2026-08-08 | E-03 | Specified but did not implement two changes needed in `apps/api` — allowlisting `codehealth://auth` in `sanitizeRedirect` (`authService.ts`) and branching `GET /auth/github/callback` to a `302` with `?token=` when `state` carries that redirect, versus the existing `200` JSON response the web app still relies on — and handed the exact diff to Rumesh instead of writing it myself | `apps/api` is Rumesh's app per the ownership rules, and GitHub's registered `redirect_uri` (`GITHUB_OAUTH_CALLBACK_URL`) points straight at his callback route; only that route holds the client secret needed to exchange a code for a token, so there's no way to finish this OAuth leg from the mobile side alone | Why can't the token exchange happen entirely on the mobile side without a backend change? |
| 2026-08-08 | E-03 | Confirmed `tsc --noEmit` passes for `apps/mobile`; have not run the real GitHub round trip, SecureStore persistence across an app restart, or the expired-token fallback to the login screen, since all three need the backend redirect change above to actually reach `codehealth://auth` with a token | A clean type-check only proves the SecureStore/fetch/WebBrowser calls are shaped correctly, not that GitHub actually redirects back into the app or that a restored session survives a real app relaunch; task is implementation-complete but not verification-complete until Rumesh's side lands | What could type-check cleanly here but still fail the first time someone actually taps "Continue with GitHub"? |
| 2026-08-16 | E-03 | Implemented the `apps/api` backend piece specified on 2026-08-08 — widened `sanitizeRedirect` in `authService.ts` to also accept the `codehealth://` scheme, threaded `statePayload.redirect` through `handleGithubCallback`'s return value, and changed `GET /auth/github/callback` in `routes/auth.ts` to `res.redirect()` with `?token=` appended instead of returning `200` JSON | This is the exact diff already handed to Rumesh as a spec on 2026-08-08 rather than written directly, since `apps/api` is his app per the ownership rules; wrote it directly this time at his request instead of handing over another diff | Why was this written directly in `apps/api` this time instead of handed to Rumesh as a diff like on 2026-08-08? |
| 2026-08-16 | E-03 | Confirmed `tsc --noEmit` passes for `apps/api` after the change; have not run the real GitHub OAuth round trip or confirmed `WebBrowser.openAuthSessionAsync` actually resolves `type: 'success'` against a live login, and no automated test exists yet for the callback route's redirect behavior | Same gap as the 2026-08-08 mobile-side entry — a clean type-check proves the redirect URL is shaped correctly, not that GitHub's browser flow actually lands back in the app with a token; task is implementation-complete but not verification-complete | What could type-check cleanly here but still fail the first time someone taps "Continue with GitHub" on a real device? |
| 2026-08-17 | E-03 | Added `apps/mobile/lib/apiClient.ts` and `authTokenStore.ts` (axios instance, request/response interceptors, single-flight refresh, SecureStore-backed refresh token) and rewrote `AuthContext.tsx` to go through them instead of raw `fetch` calls — login, logout, and the mount-time session restore all route through the new client now | `apps/web` already has this exact pattern proven out (`apiClient.ts`/`authTokenStore.ts` with interceptor-based refresh), so porting it instead of inventing a mobile-specific approach keeps both clients consistent, with the cookie-based refresh swapped for SecureStore since mobile has no cookie jar | Why keep the in-memory access token separate from the SecureStore-persisted refresh token instead of storing both the same way? |
| 2026-08-17 | E-03 | Running an actual login on a real device surfaced two bugs `tsc --noEmit` couldn't have caught: `login()` never sent `?client=native`, so the backend silently took the web/cookie branch instead; and `sanitizeRedirect` in `authService.ts` only kept strings starting with `/`, dropping the `exp://.../--/auth` deep link before the 2026-08-16 redirect fix ever had a target to redirect to. Fixed both | Allowlisted `codehealth://` and `exp://` explicitly in `sanitizeRedirect` rather than accepting any absolute URL, so the existing "otherwise this is an open redirect" comment on that function stays true — only the app's own scheme and the Expo dev-client scheme are new, not arbitrary hosts | Why does `sanitizeRedirect` need to allow `exp://` as well as `codehealth://`, and not just the app's real scheme? |
| 2026-08-17 | E-03 | Verified end-to-end on a real Android device through Expo Go: full login round trip (GitHub authorize → callback redirect → deep link back into the app → `/auth/me`), the browser sheet closing itself instead of hanging open, and session restore after a full force-quit/relaunch via the SecureStore-persisted refresh token. Closes the verification gap the 2026-08-16 entry left open | Neither bug in the row above would have shown up in a type-check — both only surfaced once a browser actually followed the real redirect chain on a device, which is why a live run was the actual bar here, not `tsc --noEmit` passing | What would have shipped broken if this had been called done on `tsc --noEmit` alone, the way the 2026-08-16 entry flagged as the known gap? |
| 2026-08-18 | D-11 | Extended `RepositoryOverviewPage.tsx`'s debt fetch to keep the `breakdown` field the `/debt` endpoint already returned, and added a `Pie`/`Cell` donut chart plus a legend under a new "Debt Breakdown" card, reusing the existing `ResponsiveContainer`/`Tooltip` styling from the page's health-trend area chart | The `breakdown` object (5 categories, zero-filled, count + debtMinutes each) was already being fetched and discarded — this only added a consumer for data the A-14 endpoint had shaped for exactly this chart | Why does the center-of-donut total use `totalDebtMinutes` from the snapshot instead of summing the breakdown values shown in the legend? |
| 2026-08-18 | D-11 | Colored 4 of the 5 slices with the existing `--destructive`/`--info`/`--warning`/`--success` theme tokens, but hardcoded Code Smell to a fixed violet (`#a78bfa`) instead of `--primary` | `--primary` (162°) and `--success` (145°) sit close enough in hue that their legend dots read as the same green in the dark theme, which would have made a category with real debt minutes (Code Smell) visually indistinguishable from one that was often zero (Maintainability) | Why not add a sixth theme token instead of hardcoding one chart-only color? |
| 2026-08-18 | D-11 | Verified against real seeded data (`demo_developer` dev-login, `seed-acme/code-health-demo` repo) rather than just `tsc --noEmit` — confirmed the donut renders only the 3 nonzero categories, the legend minutes match the seeded `Finding` rows exactly, and the zero-value categories still appear in the legend without a visible slice | A clean type-check would have proven the `breakdown` shape lined up with the extended type, not that Recharts actually draws a correct proportional donut or that the zero-fill from A-14 renders sensibly with no slice instead of erroring | What would a nonzero-but-untested category (e.g. Duplication) have looked like on the chart if the zero-fill from A-14 had been broken? |
| 2026-08-20 | D-12 | Built `HotspotTable.tsx` — a client-side sortable table (rank, file, total issues, new issues, debt) consuming the already-implemented `GET /api/repos/:repoId/hotspots` (A-15) — and wired it into `RepositoryOverviewPage.tsx` as a new "Hotspots" card, with row-click navigating to `/repositories/:repoId/findings?file=...` | The backend ranks by `totalFindings` desc and takes no sort-column query param, so per-column sorting has to happen client-side on the fetched array rather than by re-fetching; reused the existing `DataTable`/`EmptyState` primitives from `components/ui` instead of a one-off table markup, matching every other list on the page | Why does sorting re-derive the rank column instead of trusting a `rank` field from the API response? |
| 2026-08-20 | D-12 | While wiring this up, discovered the "Debt Breakdown" donut chart from D-11 (commit `12e870f`, merged via PR #72) had been silently dropped by a later merge commit (`ac358d1`, merging `develop` into `feature/nethmi-frontend-ui`) that resolved a conflict on `RepositoryOverviewPage.tsx` by keeping the redesigned-UI branch's version of the file wholesale, discarding the donut JSX while leaving its now-unused `Pie`/`PieChart`/`Cell` imports behind as the only trace; ported the original JSX back in, adapted to the current `Card`-based layout | A merge conflict resolved by picking one side's entire file instead of combining both sides' independent changes silently deletes whichever side didn't win — `git log`/`git show` on the commit that touched the file right after D-11 landed was what surfaced it, not a runtime symptom | How would this have been caught earlier, before it needed a git-archaeology session to notice? |
| 2026-08-20 | D-12 | Found and fixed an unrelated pre-existing bug blocking end-to-end verification: `RepositoriesPage.tsx`'s `fetchRepos` was a plain function re-created every render but listed as its own `useEffect` dependency, causing an infinite fetch loop that looked like a permanently-stuck "Loading repositories from Postgres…" spinner rather than a crash; wrapped it in `useCallback` keyed on `selectedOrg` | The loop wasn't visible from the code alone without actually running the page — `tsc --noEmit` has no opinion on `useEffect` dependency correctness, only real interaction (or the Network tab showing repeated `xhr` requests) exposed it | Why didn't a clean `tsc --noEmit` catch a bug this disruptive? |
| 2026-08-20 | D-12 | Verified against real seeded data end-to-end (not just `tsc --noEmit`): fixed `DATABASE_URL` port mismatches in `apps/api/.env` and `packages/db/.env` (pointed at `5432`, `docker-compose.yml` maps Postgres to `5433`), ran `prisma migrate dev` + `prisma db seed`, logged in via `/auth/dev-login` as the seeded `demo_developer` (owner of `seed-acme`), and confirmed the Hotspots card renders the 3 seeded findings ranked correctly | The `.env.example` files still document port `5432` as the default, which doesn't match this project's `docker-compose.yml` mapping — worth fixing in `.env.example` itself so the next person setting up local dev doesn't hit the same silently-hanging Postgres connection | Should `docker-compose.yml` be changed to map Postgres to the standard `5432` instead, so `.env.example`'s default is correct without editing it? |
| 2026-09-01 | A-22 | Implemented `GET /api/mobile/summary` and `GET /api/mobile/repos/:repoId/smells` — new `mobileService.ts` (`getMobileSummary`, `getRepoSmells`) and `routes/mobile.ts`, mounted in `app.ts`; exported the previously-private `getActiveRepo` from `repoService.ts` so the new service could reuse the same repo-existence guard instead of duplicating it | Following the same thin route → service function → `AppError` pattern as every other repo-scoped endpoint kept these two mobile endpoints consistent with the rest of the API instead of inventing new structure for the first mobile route file | Why does `/smells` reuse `getActiveRepo` instead of writing its own check? |
| 2026-09-01 | A-22 | Decided the smells endpoint returns every `Finding` row for the latest snapshot, not just `category: CODE_SMELL`, after noticing `api_design.md`'s own example response includes a `rule: "complexity"` finding — that's `FindingCategory.COMPLEXITY`, not `CODE_SMELL` — inside the `smells` list | Filtering strictly to `CODE_SMELL` would have contradicted the doc's own example response; treating "smells" as the mobile-card umbrella term for any finding matches what the spec actually shows, not just what the endpoint's name implies | What would the response look like for a repo whose only issues are complexity/duplication findings if this had filtered by `category: CODE_SMELL`? |
| 2026-09-01 | A-22 | Computed `scoreChange` as `latest.healthScore - previous.healthScore` from the two most recent `HealthSnapshot` rows, defaulting to `0` when only one snapshot exists, since no `scoreChange`/`healthScoreChange` field exists anywhere in the schema | `api_design.md`'s example shows a nonzero `scoreChange` but doesn't define how it's derived; comparing the two latest snapshots directly keeps the number honest to what's actually stored, rather than adding a persisted delta column to `HealthSnapshot` for one field this task didn't ask for | Why not add a persisted `scoreChange` column to `HealthSnapshot` instead of computing it on every request? |
| 2026-09-01 | A-22 | Verified both endpoints against real seed data (dev-login as `seed-admin`) rather than just `tsc --noEmit`: confirmed `/summary`'s repo list, `healthScore`, and `openPRs` matched the seeded `code-health-demo` repo, and `/smells` returned the 3 seeded findings sorted worst-severity-first with `totalSmells`/`newSmells` matching the raw `Finding` counts; also confirmed `limit=abc`, `limit=101`, and a nonexistent `repoId` all return the correct `400`/`404` error shapes | A clean type-check only proves the Prisma query shapes compile, not that the aggregation, ordering, or validation boundaries are actually correct against live data — same gap flagged in earlier hotspots/debt log entries | What would `scoreChange` have shown if the two latest seeded snapshots hadn't happened to land on the same capped `healthScore`? |
| 2026-09-06 | D-15 | Rewrote `RepositoryQualityGatePage.tsx` — a real 0-100 slider for `minHealthScore` synced to its number input, an explicit "No limit" checkbox for each of the five nullable thresholds instead of relying on an emptied text box, a "Reset to Defaults" button, and moved the existing `blockPR` toggle out of the card header into its own row with an explanatory caption | The file already had a working GET/PUT form from an earlier UI-components pass; matching its existing hand-rolled-toggle and inline-Tailwind conventions kept the diff to one file instead of introducing a new `Slider`/`Switch` primitive into `components/ui` that nothing else in the app currently needs | Why not add reusable `Slider`/`Switch` components to `components/ui` instead of keeping both controls inline in this one page? |
| 2026-09-06 | D-15 | Added a Gate Status banner and a Gate History list to the same page, both fed by a new parallel fetch to the already-existing `GET /api/repos/:repoId/pulls` (`Promise.allSettled` alongside the quality-gate fetch, same pattern `RepositoryOverviewPage.tsx` already uses for multiple calls) | That endpoint already returns per-PR score, pass/fail status, and time; reusing it meant the wireframe's status/history sections needed zero changes to `apps/api` or the schema, keeping the whole task inside `apps/web` | Why does this page never compute PASS/FAIL itself from the form's current threshold values, only ever display the backend's own result? |
| 2026-09-06 | D-15 | Fixed a bug in the existing code while rewriting it: `gate` state started as `null` and `handleSave` silently no-opped (`if (!repoId \|\| !gate) return`) whenever the initial load was slow or failed; initialized it with real defaults instead so Save always has something to send | The page's own acceptance criteria assumes Save always works; a state shape that can leave it silently dead on a slow network was a correctness gap in the page this task was already rewriting, not a separate task | How would this bug have shown up to someone testing the acceptance criteria, if it had shipped? |
| 2026-09-06 | D-15 | Kept sending all 7 `QualityGate` fields on every PUT, never a partial body | `validateRequest` (`apps/api/src/middleware/zodValidate.ts`) parses the body against the Zod schema purely to validate, then discards the parsed/defaulted result instead of writing it back onto `req.body` — so an omitted `minHealthScore`/`blockPR` would reach the handler as `undefined`, and Prisma's `upsert` treats that as "leave unchanged" on an existing row rather than falling back to the schema default. Always sending the full object sidesteps that gap entirely | What would happen to an existing gate row if the form only sent the fields the user had actually touched? |
| 2026-09-06 | D-15 | Found two unrelated pre-existing TypeScript errors while confirming the build (`LoginPage.tsx`'s missing `SIGN_OUT_MESSAGES`, a Recharts tooltip type mismatch in `RepositoryOverviewPage.tsx`) that block `npm run build -w web` for the whole app; confirmed via `git status` that neither file was touched this session, then left both alone | Neither file is part of what D-15 asked for, and the project's own rule is to touch only what the task needs and flag pre-existing issues rather than absorb them into an unrelated diff; `npm run dev -w web` isn't gated by `tsc`, so they didn't block manually verifying this page | Since you found these, why didn't you just fix them while you were in the codebase anyway? |
| 2026-09-06 | D-15 | Verified end-to-end against real seeded data (dev-login as `seed-admin`, working around a local Windows networking issue that was blocking the real GitHub OAuth callback on this machine, unrelated to this task) — confirmed an existing gate's saved values pre-fill correctly, the slider and number input stay in sync, each "No limit" checkbox persists as `null` after save and reload, the `blockPR` toggle persists, "Reset to Defaults" only changes local state until Save is clicked, and the Gate Status banner/Gate History list match the existing Pull Requests page for the same repo | `tsc -b` and `oxlint` passing only proves the types and lint rules are satisfied, not that the slider sync, the No-limit/null round-trip, or the Reset-doesn't-autosave behavior actually work against a live API — same gap flagged in earlier log entries for endpoint-only tasks | How did you confirm "Reset to Defaults" doesn't silently save to the server the moment it's clicked? |
| 2026-09-09 | A-26 | Implemented rate limiting middleware — new `middleware/rateLimit.ts` exporting four `express-rate-limit` instances (global 100/15min, webhook 30/1min, auth 20/15min, analyze-trigger 5/5min per user) backed by a `rate-limit-redis` `RedisStore`, wired into `app.ts`, `routes/webhooks.ts`, `routes/auth.ts`, and `routes/repos.ts` | `system_architecture.md` §7.1 already specified this exact design (library, Redis-backed store, the 3-tier window/max table); the `/auth/*` tier is a fourth addition closing `api_design.md` Gap #9, which flagged `/auth/refresh`/`/auth/dev-login` as unrated but didn't specify a number, so 20/15min per IP was my own call | Where did the 20-requests-per-15-minutes number for the `/auth/*` limiter come from, since it isn't in either doc? |
| 2026-09-09 | A-26 | Reused the existing shared `ioredis` client from `lib/redis.ts` for the store instead of opening a second Redis connection, via a `sendCommand: (...args) => redis.call(command, rest)` adapter — `redis.call(...args)` alone doesn't typecheck because ioredis's overload wants a `[command, ...rest]` tuple, not a plain `string[]`, for a bare spread | Splitting the array into `command` + `rest` and passing them as two separate arguments (`call(command, rest)`) matches the overload that takes the trailing args as one array instead of spreading them, so it typechecks without a cast covering more than the necessary union | Why doesn't `redis.call(...args)` typecheck when `args` is already declared as `string[]`? |
| 2026-09-09 | A-26 | Found and fixed a real bug during manual testing, not typecheck: `authRouter.use(authRateLimiter)` had no path argument, and since `authRouter` itself is mounted at the app root (`app.use(authRouter)`, no prefix), the unscoped `.use()` ran on every request that entered the router — which is every request in the whole app — so `GET /health` started 429-ing after ~9 rapid requests in testing, counted against the 20-request auth budget instead of being exempt under the global tier's `skip`. Fixed by scoping both mounts to `authRouter.use('/auth', authRateLimiter)` / `devLoginRouter.use('/auth', authRateLimiter)` | Router-level `.use(middleware)` with no path filters nothing — it runs for every request reaching that router instance regardless of which individual routes are registered on it; the path has to be passed to `.use()` itself to scope it, matching a route path like `/auth/github` isn't enough on its own | Would `tsc --noEmit` have caught this, and if not, what would have? |
| 2026-09-09 | A-26 | Verified all four limiters against the live API + real Redis (not just typecheck): hammered `/health` 105x (all 200, confirming the fix above), hit an unscoped route 105x (100 pass, 101st+ 429), POSTed `/webhooks/github` 32x with a bad signature (30 pass, rejected before signature verification even runs on the 31st), hit `/auth/github` 22x (20 pass, 21st 429), and dev-logged-in as two different users to hit `/api/repos/:repoId/analyze` — user A got 5 through then 429 on the 6th, user B succeeded in the same window, proving the key is `req.user.id` and not the caller's IP. Cleared the `rl:*` Redis keys between each run so counts weren't polluted by earlier test traffic | A clean `tsc --noEmit` only proves the middleware options are shaped correctly, not that the Redis-backed counting, the per-route wiring, or the per-user vs per-IP keying actually behave correctly against live traffic — same gap flagged in every earlier log entry for endpoint-only tasks, and the `authRouter` bug above is a concrete example of something typecheck alone missed | What would the `/health` test have shown if the `rl:*` Redis keys from an earlier run hadn't been cleared first? |
| 2026-09-09 | A-31 | Implemented `GET /api/metrics` — new `metricsService.ts` (`getMetrics`) and `routes/metrics.ts`, mounted in `app.ts`, guarded by `requireAuth` + `requireAdmin` | Followed the same thin route → service function → `AppError`/`next(err)` pattern every other route already uses (`admin.ts` was the closest match, since it's the only other `requireAdmin`-guarded route), instead of inventing a different structure for the first purely operational (non-tenant) endpoint | Why guard this with the same `requirePlatformRole('ADMIN')` middleware used elsewhere instead of a one-off role check inside the route handler? |
| 2026-09-09 | A-31 | Sourced `analysisStats.totalAnalyses`/`averageHealthScore` from `prisma.healthSnapshot.aggregate()` rather than counting `AnalysisJob` rows by status | A `HealthSnapshot` row is written exactly once, only when a job completes successfully, so counting/averaging over `HealthSnapshot` is inherently "completed analyses only" with no extra status filter needed — `api_design.md` never states which `AnalysisJob` statuses `totalAnalyses` should include, so this sidesteps the ambiguity instead of guessing a filter | What would `totalAnalyses` have included if it had counted `AnalysisJob` rows directly instead of `HealthSnapshot` rows? |
| 2026-09-09 | A-31 | Computed `averageDurationSeconds` in JS from `AnalysisJob.startedAt`/`completedAt` on completed jobs, since no duration column exists anywhere in the schema | Averaging two datetime columns isn't a plain Prisma aggregate — the alternative was a raw `EXTRACT(EPOCH FROM ...)` SQL query, and no raw-SQL precedent for a computed average exists anywhere in this codebase (`health.ts`'s `$queryRaw` is a trivial `SELECT 1`), so fetching the rows and averaging in JS stayed inside the project's existing Prisma-only query style at this data size | Why not add a persisted `durationSeconds` column to `AnalysisJob` instead of computing it from two timestamps on every request? |
| 2026-09-09 | A-31 | Mapped BullMQ's `getJobCounts()` vocabulary onto the doc's `pending/active/completed/failed` shape by treating `pending` as `waiting` only, and left `AnalysisStatus.CANCELLED` unrepresented anywhere in `queueStats` | BullMQ has no `pending` key (it calls it `waiting`) and no `cancelled` bucket at all; keeping `queueStats` sourced purely from BullMQ's own state instead of mixing in a second query against Postgres's `AnalysisJob.status` keeps each half of the response backed by exactly one system, rather than blending two different definitions of "in progress" | What would happen to a job sitting in BullMQ's `delayed` state (e.g. between retry backoffs) under this mapping — would it show up anywhere in `queueStats` at all? |
| 2026-09-09 | A-31 | Verified `401` (no token), `403` (dev-login token for a plain `USER`), and `200` (same repo, real numbers) against the live API and real dev data — promoted the same `testadmin` user to `ADMIN` directly in Postgres via Prisma Studio partway through and re-sent the *same already-issued* access token rather than logging in again for a fresh one | `requirePlatformRole.ts`'s own comment claims it "reads the role from the database rather than the access token, otherwise a demoted admin keeps their powers until the token expires" — reusing one unchanged token across the before/after DB edit is what actually tests that specific claim; getting a brand-new post-promotion token would have proven the endpoint works for admins, but not that the role check re-reads the database on every request instead of trusting the token | Why does reusing the same token before and after the database promotion prove `requireAdmin` re-checks the database, rather than simply getting a new token after promoting the user? |
| 2026-09-10 | C-07 | Audited the schema for drift from applied migrations — ran `prisma migrate status` against the local dev DB, checked whether A-26/A-31 had touched `schema.prisma`, and searched the codebase for raw-SQL migration bypasses — then fixed the one gap found: `database_design.md`'s closing note said "captured in two migrations," missing `20260810145457_add_session_rotation` (A-34) | `project_plan.md` scopes C-07 as an ongoing audit, not a task that assumes drift already exists; running the actual checks instead of assuming there was a migration to write is what showed the schema and migrations were already in sync, and the doc fix is the one place reality had genuinely diverged from what was written down | How do you know there's no drift beyond what `migrate status` and a grep for raw SQL can see — could a change exist that neither check would catch? |
| 2026-09-11 | C-10 | Added the `Agent` model to `packages/db/prisma/schema.prisma` (`tokenHash` unique, `orgId`/`organization` FK, `lastSeenAt`, `revokedAt`, `createdAt`, `@@index([orgId])`) plus the matching `agents Agent[]` back-relation on `Organization`, then generated and applied `20260911144432_add_agent` | `analysis_access_and_reporting_design.md` §3.6 specs this exact record ("token hash, org, last-seen") as the identity a self-hosted data plane authenticates with instead of sharing DB credentials; `tokenHash` mirrors `Session.tokenHash`'s hash-only storage, and `orgId` uses `onDelete: Restrict` like `Repository.orgId` — the convention for a primary tenant-owned resource, not the `Cascade` used on join rows like `OrganizationMember` — so an org can't be deleted while a live agent still belongs to it. Token issuance and the auth path that reads this table are `A-36`, deliberately not touched here | Why does `Agent.orgId` use `Restrict` when `OrganizationMember.orgId` uses `Cascade` for what looks like the same "belongs to an org" relationship? |
| 2026-09-12 | A-39 | While scoping the orphaned-webhook fix in `unlinkRepository`, found `routes/repos.ts` had been importing `linkRepository`/`unlinkRepository`/`listAvailableRepos` from `repoService.ts`'s mock stub (hardcoded sample repos, no GitHub calls at all) since a same-day merge (`ac358d1`, Aug 18) silently reverted Rumesh's ngrok-verified real wiring (`b86baef`) back to the placeholder that `8744ecb` then patched missing exports onto instead of restoring | Reading either service file in isolation looks correct — the divergence only shows up by tracing which module the route actually imports from, which `git log --follow`/`git show` on both files made visible: they were created in the same original commit as two parallel implementations, and only one was ever meant to be live | How was this found, since `tsc --noEmit` passes either way and nothing throws at runtime? |
| 2026-09-12 | A-39 | Repointed `routes/repos.ts` back to `repoLinkService.ts` for `linkRepository`/`listAvailableRepos`/`unlinkRepository`, deleted the dead mock functions from `repoService.ts` (`getAvailableRepos`, the `listAvailableRepos` alias, `linkRepository`, `unlinkRepository`), and removed `linkRepositorySchema` from `repoSchemas.ts` — it required `name`/`fullName` in the body, which `LinkRepositoryModal.tsx` never sends, so `POST /api/repos` was returning `400 Validation failed` on every real link attempt through the UI | `LinkRepositoryModal.tsx` already sent `{ githubRepoId: number }` and expected `{ githubRepoId: string, ...isAlreadyLinked }[]` back from `/available` — exactly `repoLinkService.ts`'s contract — so restoring the old wiring needed zero frontend changes, only removing the schema/route code that had drifted from what the UI actually calls | How did linking a repo appear to work at all for the past few weeks if this schema was rejecting every real request? |
| 2026-09-12 | A-39 | Fixed `unlinkRepository`'s `deleteWebhook` catch to only treat a GitHub `404` as "already gone" (safe to null `webhookId`); any other failure now logs at `error` instead of `warn` and leaves `webhookId` set on the now-inactive row instead of discarding it | The original catch swallowed every error identically, so an expired token or a GitHub outage was treated the same as "the repo was already deleted on GitHub" and lost the only pointer to a hook that was probably still live; reused the `(err as { status?: number }).status` cast already established in `fetchRepo` rather than introducing an `@octokit/request-error` dependency nothing else in the codebase uses | Since `webhookId` is `@unique` in the schema, doesn't leaving it set on a failed unlink risk a collision if the repo is relinked? |
| 2026-09-12 | A-39 | Added a best-effort stale-webhook cleanup in `linkRepository` — before creating a new hook, it now checks the existing row's leftover `webhookId` and tries to delete it first, swallowing failures — and confirmed `npm run typecheck` passes; have not yet exercised the flow against a real GitHub repo over ngrok the way the original A-11 integration pass (step 32a) did | Without this, a repo whose earlier unlink failed non-404 would end up with two live webhooks after a relink, doubling every future delivery; type-checking only proves the Prisma/Octokit call shapes compile, not that the create/delete calls or the DB's `webhookId` unique constraint actually behave correctly against live GitHub — same verification gap flagged in every earlier endpoint-only log entry here | What would happen on relink if the stale webhook delete succeeds but the new `createWebhook` call then fails? |
| 2026-09-12 | C-11 | Added `botCommentId String?` to the `PullRequest` model in `packages/db/prisma/schema.prisma` and generated/applied the migration (`20260912065525_add_pull_request_bot_comment_id`) — a single nullable `ADD COLUMN "botCommentId" TEXT`, nothing else in the table changed | `analysis_access_and_reporting_design.md` §5.5 specs this exact field for `FR-36` (update the bot's existing PR comment on later pushes instead of posting a new one each time); left nullable with no default since a PR has no bot comment until the worker posts its first one, and an additive nullable column needed no hand-written backfill the way `orgId` did in C-09. No application code touched — the field is only consumed later by `B-19` (PR comment poster, Week 13); confirmed with `tsc --noEmit` clean on both `apps/api` and `apps/worker` | Why is this column nullable instead of a required field defaulting to an empty string? |
| 2026-09-12 | B-25 | Added Vitest as the test runner for `apps/worker` (no test framework existed anywhere in the repo yet) and wrote `score.test.ts` — 11 tests for `computeScore` in `score.ts` covering the base penalty formula, category/severity weighting, diminishing returns on repeated findings of the same rule, severity-order independence within a rule group, the jscpd double-count guard, LOC normalization for large repos, `duplicationPct` clamping, the 0-floor on `healthScore`, and that reported counts match input findings; confirmed with `npm test` (11 passing) and `npm run typecheck` (clean) | Chose Vitest over Jest since it runs TypeScript directly with no `ts-jest`/Babel config needed — the smallest addition for a package starting from zero test infra; each assertion is tied to a specific rule already named in `score.ts`'s own constants and comments (`BASE_PENALTY`, `CATEGORY_WEIGHTS`, `SEVERITY_MULTIPLIERS`, `DIMINISHING_COEFF`) rather than re-deriving the formula independently, so a failing test points at which rule broke instead of just "the total changed" | Why do some assertions check an exact score (e.g. `99.5`) while others only check a relative comparison (`toBeLessThan`)? |
| 2026-09-12 | B-18 | Added `apps/worker/src/stages/comment.ts` — `buildPrComment({ metrics, findings, baseline })`, a pure function rendering the bot's PR comment markdown: a hidden `<!-- codehealth-bot -->` marker line, a heading with the Health Score and its ▲/▼ delta vs the previous snapshot, the §7 score band label (Excellent/Good/Fair/Poor/Critical) plus the gate verdict, and a "Technical debt" section with the total in `Xh Ym`, the delta (⚠️ when debt rose, ✅ when it fell or held, "first analysis, no baseline" when there is no previous snapshot), and a per-category findings/minutes table summed from `findings[].debtMinutes` | Took the shared `SnapshotMetrics` DTO (`packages/shared`) as input rather than importing from the debt-score/matcher/delta modules (`B-14`–`B-16`), which are not on `develop` yet — this both unblocks the task and enforces `analysis_access_and_reporting_design.md` §5.4 rule 6 (the comment restates stored values, never recomputes them, so it can't disagree with the dashboard). The metrics/threshold table is deliberately left for `B-31`, and the marker line exists so `B-19`'s poster can still find its own comment if `botCommentId` is ever lost (§5.5 fallback). Not wired into `analysisProcessor.ts` — `B-19` owns that, since the gate and persist stages it needs don't exist yet | Why does the builder take `SnapshotMetrics` instead of the `ScoreResult` from `computeScore`, when both carry the health score? |
| 2026-09-12 | B-18 | Wrote `comment.test.ts` — 12 Vitest cases covering `formatMinutes` (`0m`, `1h`, `6h 20m`, sign ignored), every §7 band boundary (90/70/50/25), per-category debt sums, the first-analysis / debt-up / debt-down / no-change delta text, heading arrows with and without a baseline, PASS/FAIL/no-gate rendering, and that the category table lists only non-zero categories in fixed order; `npm test` gives 23 passing (11 from `B-25` + 12 new) and `npm run typecheck` is clean | Tests assert on exact rendered lines (e.g. `## CodeHealth — Health Score 72.4 ▼ 4.1`) rather than just "contains a number", because the comment is what evaluators and reviewers read on GitHub — a wrong arrow direction or a `-` on an increase would look correct to a loose assertion and wrong to a human. Did not render into a real GitHub PR yet: that needs Octokit and `botCommentId` handling, which is `B-19` | If `debtDeltaMinutes` is `0` on both a first analysis and an unchanged repo, how does the comment tell the two apart? |
| 2026-09-14 | B-31 | Added a `QualityGateThresholds` interface to `packages/shared/src/analysis.ts` (`minHealthScore` plus the five nullable `max*` limits, same field names as the Prisma `QualityGate` model) and extended `buildPrComment` in `apps/worker/src/stages/comment.ts` with a required `gate: QualityGateThresholds \| null` input, a `metricRows()` builder, and a `\| Metric \| Value \| Threshold \| \|` table rendered between the status line and the existing "Technical debt" section — each configured threshold gets a row with its measured value, the limit (`≥ 60`, `≤ 3`, `≤ 5%`) and a ✅/❌, plus a final "Technical debt" trend row (`▲ +45m` ⚠️ / `▼ -15m` ✅ / `—`); when the gate is `null` the output is byte-identical to `B-18` | The worker has no `QualityGate` type yet because `B-17` (the evaluator, Rumesh) isn't built, so the thresholds are a pure input to the stage — same as `baseline` for the matcher/delta stages — rather than a Prisma read, which keeps the "worker needs no database" rule and lets `B-17`/`B-19` pass the row straight through. Put the type in `packages/shared` instead of local to `comment.ts` because thresholds have to cross the API→worker boundary eventually and the evaluator needs the identical shape; it's additive only so it can't break the API (`tsc --noEmit` clean on both packages). Followed `analysis_access_and_reporting_design.md` §5.4 to the letter: failing rows sort first via a stable two-pass filter (not `Array.sort`, so equal-status rows keep the fixed order), rows with a `null` threshold are dropped, and the debt row never enters the breached count since `QualityGate` has no debt field | Why is `gate` a required nullable field instead of an optional one? |
| 2026-09-14 | B-31 | Kept the verdict line reading `metrics.gateResult` and only *appended* `— 3 of 6 metrics breached` on `FAIL`, with no suffix on `PASS`; the per-row ✅/❌ is computed in the builder by comparing value to threshold, with equality counting as a pass and duplication compared on the raw `duplicationPct` even though it's displayed rounded to one decimal | §5.4 rule 6 says the comment must restate the stored `gateResult` rather than recompute it, so the count is explanatory, not authoritative — a "PASSED — all within threshold" suffix would turn into a lie the moment `B-17`'s evaluator disagreed with the table on an edge case. Comparing the raw percentage means a repo at `5.04%` against a `≤ 5%` limit renders `5% ≤ 5% ❌`, which looks odd but is honest; comparing the rounded value would have the row say ✅ while the gate says FAIL, which is the worse contradiction because the two are on adjacent lines | If the row shows `5%` and the limit is `≤ 5%`, why is it marked ❌? |
| 2026-09-14 | B-31 | Extended `comment.test.ts` from 12 to 21 cases: a `gate()` fixture and a `SPEC_METRICS`/`SPEC_GATE` pair reproducing the §5.3 worked example verbatim, then tests for null-threshold omission, failing-first ordering with fixed order inside each group, at-limit values passing, the exact seven rendered table rows, raw-vs-rounded duplication, breach count on FAIL only and never counting the debt row, the three debt-trend states, no table when `gate` is null, and the metrics table landing above the debt-by-category table; `npm test` gives 55 passing across the worker (`debt`, `match`, `score`, `comment`) and `npm run typecheck` is clean for `apps/worker` and `apps/api`. Also rendered the spec example through `tsx` and eyeballed the markdown by hand | Added `gate: null` to every existing `buildPrComment` call rather than making the field optional, so the compiler forces `B-19`'s poster to decide explicitly whether it has thresholds — an optional field would let it silently ship a comment with no table. The spec example is asserted as an exact array of rows, not `toContain` per row, because the whole point of rule 1 is the *order* and a per-row check can't catch a sort regression | Why assert the full row array instead of checking each row is present? |
| 2026-09-17 | B-30 | Added `apps/worker/src/analyzers/todoScan.ts`: a pure `scanSource(text)` that runs one regex per line for `TODO`/`FIXME`/`HACK`/`XXX`, and `runTodoScan(repoPath)` that walks the checkout over the same source extensions `detect.ts` counts, skipping the same vendored/generated directories the other analyzers ignore, and returns `{ matches, counts }` with forward-slash relative paths | Followed the existing analyzer shape exactly — raw report from the analyzer, mapping to findings in `normalize.ts` — rather than inventing a base class. Splitting the regex core from the file walk means the matching rules are tested without touching the disk, and the walk is tested once with a temp directory holding a `node_modules` decoy | Why is there no external tool here when every other analyzer shells out to one? |
| 2026-09-17 | B-30 | The regex only matches a marker in upper case and directly after a comment opener (`//`, `#`, `/*`, `*`, `<!--`, `--`), so `// todo later`, `const TODO = 1` and `"TODO"` inside a string are all ignored; the note after the marker is trimmed and capped at 200 characters before it becomes the finding message | The category weight makes every false positive a real score deduction, so precision matters more than recall for this one — a developer who writes `TODO` in lower case is not admitting debt in the conventional way. The cap is because the message is stored per finding and a long comment block should not become a long database row | Why not match `todo` in lower case too? |
| 2026-09-17 | B-30 | Mapped markers in `fromTodoScan` to `MAINTAINABILITY` with `FIXME`/`HACK` → `MEDIUM` (10 min) and `TODO`/`XXX` → `LOW` (5 min); the `rule` is the lower-cased marker and the `tool` is `todo-scan`. Registered it in the `Analyzer` union and appended it next to `jscpd` in `detect.ts` so it runs on every repo regardless of language, then wired the processor block and a `TODO scan finished` log line | `MAINTAINABILITY` was empty for JS/TS until now, so this is the first signal into that category. Using the marker as the rule gives the scorer four groups to apply diminishing returns over, so a repo with 200 TODOs is penalised for the habit, not 200 times over. `FIXME`/`HACK` admit something is broken or worked around; `TODO` only admits something is unfinished, so they cost different amounts | Why does the tenth TODO cost less than the first? |
| 2026-09-17 | B-30 | First smoke run over this repo found zero of the three known `TODO(Backend):` comments in `apps/web/src/types/*.types.ts`, but the same lines matched when pasted into a unit test. The files use CRLF line endings, and a regex `.` in JavaScript does not match `\r`, so `(.*)$` failed on every line; fixed by splitting on `/\r?\n/` and added a CRLF test case. The same run also flagged my own explanatory comment in `normalize.ts` because it began with the words `FIXME and HACK`, so the comment was reworded | Smoke-testing against real files caught what the unit tests could not, because the test strings were all written with `\n`. The comment being flagged is the scanner doing its job — the fix belongs in the comment, not in the regex | Why did the unit tests pass while the real scan found nothing? |
| 2026-09-17 | B-30 | Added `todoScan.test.ts` (7 cases: each opener form, 1-based line numbers, lowercase and non-comment rejection, CRLF, text cap, a temp-directory walk with a `node_modules` and `.min.js` decoy, and an empty checkout) and `normalize.test.ts` (4 cases on category, severity/debt per marker, message format, and that `normalize` picks the report up). `npm test` gives 66 passing across the worker and `npm run typecheck` is clean | The mapper tests are the first tests on any `fromX` function in `normalize.ts`; they pin the severity-to-minutes numbers because those feed straight into the debt score | What happens to the Health Score if someone later changes `FIXME` to `HIGH`? |
| 2026-09-17 | E-06 | Built `screens/RepoSummaryScreen.tsx` — health score gauge with delta vs previous scan, 30-day trend, five category bars with a total row, and a top-5 issues list — fed by four existing endpoints (`/api/repos/:id`, `/trend?days=30`, `/debt`, `/mobile/repos/:id/smells?limit=5`) fetched with `Promise.allSettled`, and wired the previously inert `TouchableOpacity` on each `HomeScreen` card to open it | Every widget on the wireframe already had a shaped endpoint behind it (A-13, A-14, A-22), so the screen needed zero changes in `apps/api`; `allSettled` mirrors `RepositoryOverviewPage.tsx` on the web so a `404` from `/debt` or `/smells` on a never-scanned repo degrades to an empty card instead of blanking the whole screen | Why does a repo with no snapshot still render a score of 80 while the other three cards show empty states? |
| 2026-09-17 | E-06 | Added `@react-navigation/native-stack` (via `npx expo install`, after asking first) and nested a typed `HomeStack` (`RepoList` → `RepoSummary`) inside the existing Home tab in `TabNavigator.tsx`, rather than holding a `selectedRepo` in `HomeScreen` state and rendering the summary inline | The E-02 tab navigator has no push/pop, and faking one with local state would have lost the native header, back gesture, and the list's scroll position for free; nesting a stack per tab is the documented React Navigation pattern and keeps `Notifications` untouched | Why nest the stack inside the Home tab instead of wrapping the whole tab navigator in a stack? |
| 2026-09-17 | E-06 | Drew the gauge, trend, and category bars with plain `View`s (a thick-bordered circle, flex bars sized by `height`/`width` percentages) instead of adding `react-native-svg` for a real arc | `HomeScreen` already renders its sparkline from `View`s, so the summary reuses the same technique and palette with no new native dependency; a proper arc gauge is a visual upgrade, not something the acceptance criteria asks for, and can be swapped in later without touching the data layer | What would adding `react-native-svg` have cost beyond one `npm install`? |
| 2026-09-18 | E-06 | Testing on a real Android device through Expo Go surfaced three blockers before the screen could even be reached, none of them in the new code: the laptop's Wi-Fi IP had changed, so both `EXPO_PUBLIC_API_URL` and `GITHUB_OAUTH_CALLBACK_URL` (plus the callback registered on the GitHub OAuth App) pointed at a dead address and login hung on github.com; then the Repositories list was empty because `listOrgRepositories` requires an active `RepositoryMember` row, not just org membership, so `vidushiDew` was added to the two seed orgs and repos in the local DB; and RN 0.86 now warns that the core `SafeAreaView` is deprecated, so `HomeScreen` was switched to the `react-native-safe-area-context` one already installed | A clean `tsc --noEmit` says nothing about environment drift or seed-data access rules — the "no repos" screen looked exactly like a bug in the new navigation until the API's access filter was read; a LAN IP hard-coded in three places is a recurring setup cost worth replacing with a stable hostname later | Why did the Repositories list show nothing even though the user was an `OWNER` of an org? |
| 2026-09-18 | E-06 | Verified end-to-end on the device against the seeded `code-health-demo` repo: tap opens the summary with the repo name in the native header, score `88` matches the list pill, debt `1h 36m` and the category bars (Vulnerability 45m, Complexity 30m, Code Smell 20m) sum to the total, top issues render worst-severity-first, pull-to-refresh and the back arrow work, and the Notifications tab still mounts. The trend card correctly shows its empty state because the seed snapshots are dated 03 Aug, outside the 30-day window, which also leaves the delta at "no change" | Checking the numbers against the list screen and the web overview page, not just that the cards rendered, is what proves the four responses were mapped to the right fields; the empty trend is real data, not a bug, and would fill in on the next analysis run | What would the delta show if the trend endpoint had returned two points? |
| 2026-09-19 | TBD | Added the first test suite for `apps/api`: vitest + supertest in `apps/api/tests/`, driving `createApp()` in-process against a real Postgres database (`code_review_test`) and Redis db `1`, with one file per router (auth, orgs, repos, quality gates, snapshots, mobile, notifications, admin/metrics, webhooks, Bull Board, health, rate limits) — 244 tests | The API had no tests at all and the point of the task is tenant isolation, which lives in Prisma `where` clauses and middleware queries; mocking Prisma would only test the mock. `createApp()` was already separate from `listen()`, so supertest needs no server or port | Why a real database instead of mocking Prisma, given it makes the suite slower and needs Docker? |
| 2026-09-19 | TBD | `tests/setup/env.ts` overrides `DATABASE_URL`/`REDIS_URL` unconditionally and throws unless the database name ends in `_test` and the Redis db index is non-zero; `globalSetup.ts` creates the database if missing and runs `prisma migrate deploy`; `hooks.ts` empties every table and flushes Redis before each test | The hooks wipe everything, so pointing them at the dev database by accident (e.g. inheriting `apps/api/.env`) would destroy it — the guard makes that impossible rather than merely unlikely. Redis db `0` holds the dev API's BullMQ queue, so flushing it would eat real jobs | What stops this suite from truncating my development database? |
| 2026-09-19 | TBD | Replaced `TRUNCATE ... CASCADE` in the per-test hook with per-table `DELETE` inside one transaction under `SET LOCAL session_replication_role = replica`, falling back to TRUNCATE if the user isn't superuser | Timed it: TRUNCATE cost ~700 ms per call on Docker Desktop for Windows (it rewrites table files), DELETE ~15 ms. Disabling FK triggers for the transaction removes the need to order the deletes by dependency. The suite went from 357 s to 104 s | Why not just delete in FK order, or use one transaction per test and roll it back? |
| 2026-09-19 | TBD | Wrote `crossTenant.test.ts`: a table of every `:repoId`, `:orgId` and `:snapshotId` route (reads and writes) called with org B's owner, admin, team-lead and a no-org user against org A's ids, asserting exactly `404 { error: { code: 'NOT_FOUND' } }` and that no org-A identifier appears in the body or headers; plus a check that org A's data is unchanged after org B's writes | A 403 would confirm the resource exists, which is itself a leak, so the assertion is on 404 specifically and on the response containing nothing from the other tenant. Testing the write methods matters because a read-only matrix would miss a route that lets another org mutate data | Why must the cross-tenant response be a 404 and not a 403? |
| 2026-09-19 | TBD | Added a completeness guard that walks the live Express router (`app._router.stack`) and fails if any registered route with `:repoId`/`:orgId`/`:snapshotId` is missing from the matrix, then proved both guards by temporarily adding an unlisted route (guard failed naming it) and removing `requireRepoAccess` from `/debt` (five matrix rows failed) | A matrix that only covers the routes someone remembered to list would silently rot; deriving the expected set from the router means a new tenant-scoped route breaks CI until it is added. Deliberately breaking the code to watch the test fail is the only way to know a test tests anything | How do you know a new endpoint added next month will be covered? |
| 2026-09-19 | TBD | Flushed the rate-limiter keys before every test and asserted the limits themselves (20 on `/auth/*`, 100 global, 5 per user on `/analyze`) instead of disabling `express-rate-limit` for tests | The global limiter is 100 requests per 15 minutes per IP and supertest is always the same IP, so the suite would start returning 429 partway through; flushing keeps the real middleware in the path so the limits get tested rather than bypassed | Why did the suite 429 halfway through the first time, and why not just disable rate limiting under `NODE_ENV=test`? |
| 2026-09-19 | TBD | Pinned four pre-existing bugs with tests that assert current behaviour and a comment, rather than fixing them: `/analyze` uses `repoService.triggerManualAnalysis` (ignores the role, never enqueues a BullMQ job — the real one in `queueService` is dead code); `qualityGates.ts` skips `repoIdParamsSchema`; `hotspotsQuerySchema` is applied to `/trend` not `/hotspots`; `POST /api/repos` imports `linkRepositorySchema` but never applies it | Mixing behaviour fixes into a test PR makes both harder to review, and a pinned test turns each fix into a visible diff later instead of a silent change | Why did you write a test that asserts the wrong behaviour? |
| 2026-09-19 | TBD | Added a `test` job to `ci.yml` with `postgres:16-alpine` and `redis:7-alpine` service containers on the same host ports docker-compose uses (5433, 6380), removed the `packages/db` stub `test` script that did `exit 1`, added a root `npm test` that fans out with `--workspaces --if-present`, and made `apps/api`'s `typecheck` include the tests via `tsconfig.test.json` | CI ran no tests before this — the worker's vitest suite was never executed on a PR. The db stub script would have failed any workspace fan-out, and tests that don't type-check drift from the code they test | Why did the existing CI never run the worker tests, and what would have happened running `npm test --workspaces` before this change? |
| 2026-09-19 | TBD | Tested routes whose happy path calls GitHub (`POST /api/repos`, `GET /api/repos/available`, `POST /api/orgs/sync`) only for their 401/400/no-credential paths, and the OAuth callback only up to state/nonce validation | The repo has no HTTP mocking (no `nock`/`msw`) and adding one is a separate decision; the parts that don't need GitHub are the parts that guard tenancy anyway | Which endpoints are not fully covered and what would it take to cover them? |
| 2026-09-21 | E-10 | Extracted `LoadingState`, `EmptyState` and `ErrorState` into `apps/mobile/components/` and the `loading / refreshing / error` state machine into a `useAsyncData` hook, then rewired all three data screens onto them, instead of adding the four states to each screen separately | The screens had already drifted into three conventions — `RepoSummaryScreen` had error-and-retry, `HomeScreen` had neither, `NotificationsScreen` swallowed failures into an empty array — so writing a fourth variant per screen would have set the inconsistency in place permanently; with one hook and three presentational components, changing how a loading or empty state looks is now a single edit rather than three, and the naming matches the `LoadingState` / `EmptyState` / `ErrorState` trio the web app already has in `apps/web/src/components/ui/` | Why put the state machine in a hook rather than a wrapper component that renders its children? |
| 2026-09-21 | E-10 | Kept each screen's own fetch body and passed it into `useAsyncData` as a closure, rather than adding `@tanstack/react-query` — which the web app already depends on — to the mobile app | The three fetches have genuinely different shapes: `HomeScreen` chains orgs → repos → a per-repo trend fan-out, `RepoSummaryScreen` uses `Promise.allSettled` so three of its four calls can fail without failing the screen, and `NotificationsScreen` is a single GET; all three are mount-once-then-pull-to-refresh, so the caching, background refetching and query invalidation that justify react-query on the dashboard would have been unused weight in a nine-file app, and the hook that replaces it is about fifty lines | At what point would the mobile app actually start to need react-query? |
| 2026-09-21 | E-10 | Found that `HomeScreen.fetchRepos` wrapped its three requests in a `try` with a `finally` but no `catch`, so a failed request became an unhandled promise rejection and left the screen showing an empty list indistinguishable from “no repositories”, and fixed it by moving the fetch into the hook, which catches and maps the error through a new `getErrorMessage` helper | The bug was invisible in normal use because the `finally` still cleared the loading flag, so the screen always reached a plausible-looking state and the rejection only surfaced in the Metro console; routing every fetch through one `catch` means a screen cannot silently present a failure as an empty result, which is the specific confusion this task exists to remove | Why did a missing `catch` never show up as a crash or an error screen? |
| 2026-09-21 | E-10 | Rendered the empty state inside the list through `ListEmptyComponent` and added `flexGrow: 1` to the `contentContainerStyle` when the data array is empty, instead of swapping the `FlatList` out for a centered `View` | A `FlatList` with no rows has no content height, so on Android the `RefreshControl` has nothing to pull against and the gesture silently does nothing; swapping the list out for a plain view removes the gesture entirely, and an empty repository list is precisely the state a user most wants to refresh from, because it is usually empty only until the repo linked on the dashboard finishes syncing | Why does `flexGrow: 1` on the content container restore the pull gesture when `flex: 1` on the list itself does not? |
| 2026-09-21 | E-10 | Split failure into two presentations — a full-screen `ErrorState` with a Retry button when a fetch failed and there is no data, and a compact banner above the still-rendered content when a pull-to-refresh failed — with `useAsyncData` deliberately keeping the previous data on a refresh error | Blanking a populated screen because a background refresh timed out discards information the user could still read and act on, and it punishes the refresh gesture; keeping the stale rows and admitting the failure in a banner is the honest state, and it also keeps the `RefreshControl` reachable so the retry is available as both a button and the gesture the user already made | Why keep stale data on screen instead of replacing it with the error the request actually returned? |
| 2026-09-21 | E-10 | Wired up `NotificationsScreen`'s existing but unreachable list — the `FlatList`, the `PanResponder`-based `SwipeableItem`, the header and `markAllRead` were all written under `E-07` but the component returned a placeholder `<Text>Notifications</Text>`, so none of it had ever rendered — and converted its light-theme `StyleSheet` to the dark tokens the other two screens use | `E-10` delivers four data states across three screens, and they cannot be demonstrated or reviewed on a screen whose body is a placeholder; the alternative was to wrap loading and empty branches around a stub, which would have passed a typecheck and shown nothing on a device. This overlaps `E-07` (step 64, Nethmi) and needs flagging in review rather than being absorbed silently — what stays open there is the mutation layer, where `handleMarkRead`, `handleDismiss` and `markAllRead` each apply the same optimistic update in their `catch` as on success, so a failed write still looks like it worked | Which parts of the notification screen does `E-10` close, and what remains `E-07`'s to finish? |
| 2026-09-21 | E-10 | Added `apps/mobile/theme.ts` with the colour, spacing and radius tokens, but used it only in the new components, `NotificationsScreen` and `TabNavigator`, leaving the hardcoded hex in `HomeScreen` and `RepoSummaryScreen` untouched | Replacing roughly nine hundred lines of existing `StyleSheet` literals in the same commit would have buried the four states this task is actually about under a mechanical find-and-replace, and made the diff unreviewable; scoping the tokens to new code and to the one screen that had to change theme anyway establishes the convention without spending the review budget on churn | Why not finish the token migration while those files were already open? |
| 2026-09-22 | B-20 | Posted the quality gate verdict to GitHub's Commit Statuses API as `success` or `failure` on every run, regardless of whether the repository has PR blocking switched on | `FR-33` reads "when PR blocking is enabled ... post a failing commit status", not "only then", and `FR-34` attaches no blocking condition at all; since `DEFAULT_GATE.blockPR` is `false`, gating the post on it would have left every repository nobody has configured with no status at all and broken `FR-34`. Blocking is GitHub's decision to make — a status only stops a merge once an administrator marks `codehealth/quality-gate` a required check in branch protection — so the worker reports the verdict and stays out of the enforcement question. The settings screen currently labels the checkbox "Posts failing commit status on GitHub", which this makes inaccurate and needs correcting | If `blockPR` no longer controls whether the status is posted, what does it actually do? |
| 2026-09-22 | B-20 | Split the stage into `createGateStatus`, which takes the Octokit client as its first argument, and `postCommitStatus`, which reads the job from the database and resolves the owner's token; only the first is unit tested | Passing the client in lets the test assert on the exact arguments of the API call against a small hand-written stand-in, with no network and no module mocking — the same split the PR comment poster uses, so the two GitHub writers stay the same shape. The database half is left to the end-to-end check because testing it would mean asserting on a Prisma query rather than on behaviour | Why not mock the Octokit module in the test instead of passing the client in? |
| 2026-09-22 | B-20 | Caught every error from the status call, logged it, and returned, rather than letting it propagate out of the processor | The snapshot is already persisted by the time this runs, so throwing would tell BullMQ to retry the entire job — re-cloning the repository and re-running every analyzer — to retry a single HTTP call, and would eventually mark a run `FAILED` whose results were stored correctly. A revoked token or a rate limit should cost the status only | What would a revoked GitHub token cost if this stage threw instead of logging? |
| 2026-09-22 | B-20 | Sent the sha returned by the clone stage instead of the one carried on the queued job | Manual runs are enqueued with the literal placeholder `'HEAD'`, which is not a commit sha GitHub would accept; the cloned sha is what was actually checked out and what the results were persisted against, so the status lands on the same commit as the snapshot it describes | What would a manually triggered run's status attach to if the queued sha were used? |
| 2026-09-22 | B-20 | Posted on the commit for every run, including pushes and manual runs, rather than returning early when the job has no linked pull request the way the comment poster does | A commit status hangs off a sha, not a pull request, so the early return the comment poster needs — a comment has nowhere to go without a PR — does not apply here. Dropping it means a push to the default branch gets a visible verdict in the commit list, at no extra cost | Why does the comment poster return early on a manual run when this one does not? |
| 2026-09-22 | B-20 | Stored no identifier for the created status, despite `system_architecture.md` listing `statusId` as an output of this pipeline stage | The Statuses API is keyed by `context`, so a re-run replaces the previous `codehealth/quality-gate` status on that commit by itself — unlike the PR comment, which needs `botCommentId` stored because a second call would otherwise post a duplicate. Persisting an id would have meant a schema change and a migration to hold a value nothing ever reads back | How does a re-run avoid stacking up duplicate statuses without tracking the previous one? |
