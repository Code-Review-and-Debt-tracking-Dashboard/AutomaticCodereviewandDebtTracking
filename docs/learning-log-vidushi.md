| Date | Task ID | What it does  | Why this approach  | Question I'd get asked |
|---|---|---|---|---|
| 2026-07-19 | P0 | Designed the full Prisma schema in `packages/db/prisma/schema.prisma` with 12 models: User, GitHubCredential, Session, Repository, RepositoryMember, PullRequest, AnalysisJob, HealthSnapshot, Finding, QualityGate, Notification, Device | Schema-as-code keeps the DB structure in version control, reviewable in PRs, and auto-generates a type-safe TypeScript client — no hand-written SQL needed | Why use Prisma instead of writing SQL directly? |
| 2026-07-19 | P0 | Added 12 enums (PlatformRole, RepositoryRole, MemberStatus, AnalysisStatus, AnalysisTrigger, Severity, FindingCategory, FindingState, PRStatus, GateResult, NotificationType, DevicePlatform) to the schema | Enums map to PostgreSQL native enum types so the database itself rejects invalid values — not just the application layer | What is the difference between Severity and FindingCategory? |
| 2026-07-19 | P0 | Added @@index directives on frequently queried fields such as [repoId, status], [userId, readAt], and [repoId, calculatedAt(sort: Desc)] across all models | Without indexes every filtered query does a full table scan which gets very slow at scale — composite indexes speed up the dashboard queries that filter and sort on multiple fields | Why index calculatedAt descending instead of ascending? |
| 2026-07-19 | P0 | Stored GitHubCredential.encryptedAccessToken as encrypted and Session.tokenHash as a hash of the token, never the raw value | If the database is ever breached, attackers cannot use the stolen tokens directly — reversing proper encryption or a hash is computationally infeasible | Why store a hash of the session token instead of the token itself? |
| 2026-07-19 | P0 | Ran `npx prisma migrate dev --name init` — Prisma generated migration.sql and applied it to local PostgreSQL database code_review_db, saving the file under prisma/migrations/20260719051244_init/ | migrate dev creates and applies the migration in one step during development; in production migrate deploy is used instead so it only applies existing files without generating new ones | What is the difference between migrate dev and migrate deploy? |
| 2026-07-19 | P0 | Verified DATABASE_URL is excluded from Git via .gitignore at both root and packages/db level; created .env.example as a safe template for teammates | Real credentials must never be committed to GitHub even in private repos — .env.example documents which variables are needed without exposing real values | What is the difference between .env and .env.example? |
| 2026-07-19 | P0 | Staged and committed 8 new files (schema, migration SQL, migration lock, package.json, prisma.config.ts, .gitignore, .env.example, package-lock.json) and pushed to the remote devops-vidushi branch on GitHub | Feature branches isolate work-in-progress from main so incomplete changes do not break the shared codebase — work is merged only after review | Why do we commit migration files to Git at all? |