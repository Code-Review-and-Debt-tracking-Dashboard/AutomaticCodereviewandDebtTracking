| Date | Task ID | What it does | Why this approach | Question I'd get asked |
|---|---|---|---|---|
| 2026-07-19 | P0 | Designed the Prisma schema in `packages/db/prisma/schema.prisma` with 12 models covering users, authentication, repositories, pull requests, analysis jobs, health snapshots, findings, quality gates, notifications, and mobile devices | Keeping the schema in code makes database changes version-controlled and reviewable, while Prisma generates a type-safe client for the TypeScript applications | Why use Prisma instead of writing SQL directly? |
| 2026-07-19 | P0 | Added 12 enums for roles, statuses, severity, finding categories, notification types, and mobile platforms | PostgreSQL enums restrict these fields to known values and prevent invalid application data from being stored | What is the difference between `Severity` and `FindingCategory`? |
| 2026-07-19 | P0 | Defined model relations, foreign keys, unique constraints, and delete behavior such as `Cascade`, `Restrict`, and `SetNull` | Explicit constraints preserve data integrity and clearly define what happens to dependent records when parent records change or are deleted | Why do different relations use different `onDelete` behaviors? |
| 2026-07-19 | P0 | Added composite indexes for common dashboard queries, including repository status, notification read state, and health snapshots ordered by calculation time | These indexes support the fields used together for filtering and sorting, reducing expensive table scans as data grows | Why use a composite index instead of separate indexes for every field? |
| 2026-07-19 | P0 | Designed secure credential fields: GitHub access tokens are stored encrypted and session tokens are stored as hashes rather than raw values | An encrypted token can only be recovered with the encryption key, while a one-way token hash allows session verification without storing the original session token | Why encrypt an access token but hash a session token? |
| 2026-07-19 | P0 | Ran `npx prisma migrate dev --name init`, generating the initial `migration.sql` and applying it to the local PostgreSQL `code_review_db` database | `migrate dev` creates and applies migrations during development; production should use `migrate deploy` to apply reviewed migration files | What is the difference between `prisma migrate dev` and `prisma migrate deploy`? |
| 2026-07-19 | P0 | Added `prisma.config.ts`, the migration lock file, and `.env.example`, while keeping the real `.env` database credentials out of Git | The configuration gives Prisma a consistent schema and migration location, and `.env.example` documents setup without exposing secrets | Why commit `.env.example` but not `.env`? |
| 2026-07-19 | P0 | Committed the database package and migration files on the `devops-vidushi` feature branch and submitted the work through pull request #8 | A feature branch and pull request keep incomplete work isolated and allow review before it reaches the shared `develop` branch | Why should migration files be committed to Git? |
| 2026-07-25 | P0 | Created `packages/db/prisma/seed.js` with linked demo records for 10 models: users, repository membership, pull request, analysis job, health snapshot, findings, quality gate, notification, and mobile device | Fixed `seed-*` identifiers and Prisma `upsert` operations make the seed predictable and safe to run repeatedly; only seed-owned findings and notifications are replaced | Why use fixed values and `upsert` instead of generating completely random records? |
| 2026-07-25 | P0 | Deliberately left `GitHubCredential` and `Session` out of the shared seed | These authentication tables hold encrypted access tokens and token hashes; fake records would test relationships but would not provide valid GitHub authentication and could be mistaken for usable credentials | Why does the seed not insert data into every table? |
| 2026-07-25 | P0 | Added `db:generate`, `db:migrate`, `db:seed`, and `db:validate` package scripts and registered the seed command in `prisma.config.ts` | Named scripts give every teammate the same short, documented commands for common Prisma operations | How does Prisma know which seed file to execute? |
| 2026-07-25 | P0 | Replaced the incompatible TypeScript 7/`ts-node` seed runner with Node.js and configured Prisma 7 with `@prisma/adapter-pg` and `pg` | Prisma 7 requires an explicit PostgreSQL driver adapter, and plain JavaScript avoids the `ts-node` compiler incompatibility without changing the database logic | Why does Prisma 7 need a PostgreSQL adapter passed to `PrismaClient`? |
| 2026-07-25 | P0 | Ran the seed repeatedly and validated the result in local PostgreSQL: 2 users, 1 repository, 1 snapshot, 3 findings, and 1 notification | A second successful run with unchanged counts verifies that the seed is idempotent and does not accumulate duplicate demo data | How did you verify that rerunning the seed does not create duplicates? |
| 2026-07-27 | C-05 | Bulk-inserted throwaway data (4 extra repos, ~1,000 snapshots, ~4,000 findings, 2,000 "noise" repos) before running `EXPLAIN ANALYZE`, then deleted it all and confirmed row counts matched the C-04 seed exactly | The C-04 seed only has a handful of rows per table; Postgres correctly avoids using an index on a 3-row table regardless of how good the index is, so validating against it would have shown Seq Scans everywhere and proven nothing | Why not just run `EXPLAIN ANALYZE` against the existing seed data? |
| 2026-07-27 | C-05 | Measured the trend query (`HealthSnapshot` by repo + date range) and both findings queries against real data and recorded the actual plans in `database_design.md`, replacing the old reasoning-only "Verified ✅" notes | The original notes were justified from the index definitions on paper, not from a real query plan; C-05's whole point is to check that reasoning against what Postgres actually does | What's the difference between saying an index "should" be used and proving it "is" used? |
| 2026-07-27 | C-05 | Found that the findings state/severity breakdown query doesn't actually use `[snapshotId, state]` — it uses `[snapshotId, file]` instead, because both indexes share `snapshotId` as the leading column and the query has no filter beyond that | Composite indexes are only "needed" for their second column when a query filters on it; an index whose second column is never a WHERE condition here (state/severity are only in GROUP BY) doesn't get chosen for that reason, and any sibling index sharing the same leading column would do just as well | If `[snapshotId, state]` isn't being used here, does that mean it's dead weight? |
| 2026-07-27 | C-05 | Constructed the `GET /api/repos` list query from the `api_design.md` contract (it isn't implemented yet) and added it to `database_design.md` as Query 7 so it has a documented, index-checked shape before someone builds the route | C-05 asked me to validate a "repo list" query, but no such query existed yet in the docs or the codebase to validate — writing it down now means the route implementer starts from a checked query instead of inventing one from scratch | Isn't writing a query for an unbuilt route getting ahead of the actual implementation task? |
| 2026-07-27 | C-05 | Padded `Repository` to 2,005 rows specifically to test the repo-list query's owner/member filter, and found Postgres chooses a Seq Scan over the `ownerId` index even at that size — documented this as expected planner behavior, not a bug | A `Seq Scan` over an indexed column looks like a red flag at first glance, but `OR EXISTS(...)` can't collapse into one index condition, and scanning 2,000 rows (54 pages) is genuinely cheaper than two index scans plus a bitmap merge; the query still ran in under 1ms | How do you tell a real missing-index problem apart from the planner correctly picking a sequential scan? |
| 2026-07-29 | A-13 | Implemented `GET /api/repos/:repoId/trend` — `repoService.ts` (repo existence check, query parsing, the Prisma query) plus `routes/repos.ts` and mounting it in `app.ts` — as the first `/api/repos/*` endpoint in the codebase, since no repo router or repo-scoped auth check existed yet | Following the existing `auth.ts`/`webhookService.ts` pattern (thin route → service function → `AppError` on failure) kept the endpoint consistent with the rest of the API instead of inventing a new structure for the first repo route | Why does this route only check that the repo exists, not that the caller owns or is a member of it? |
| 2026-07-29 | A-13 | Made `from`/`to` win over `days` when a request sends both, and treat a request with only one of `from`/`to` as a `400`, since `api_design.md` doesn't say what happens when both params are present | An explicit date range is a more deliberate signal than the `days` shorthand, so the more specific input should win; a half-supplied range is a client mistake, not a valid shorthand, so it should fail loudly instead of silently falling back to the 30-day default | What would happen if a client sent `?days=7&from=2026-01-01&to=2026-01-31` before this decision was made? |
| 2026-07-29 | A-13 | Queried `HealthSnapshot` with `calculatedAt: { gte: from, lte: to }` instead of `gt`/`lt` | A snapshot calculated at exactly the `from` or `to` boundary is still inside the requested range from the caller's point of view; `gt`/`lt` would silently drop it, an off-by-one bug at the edge of the range | Why would using `gt` instead of `gte` here be a bug and not just a stylistic choice? |
| 2026-07-29 | A-13 | Verified all four cases (`200`/`401`/`404`/`400`) against real local Postgres by hand-signing a JWT with `jsonwebtoken` using the same `JWT_SECRET` the running server had loaded, instead of going through `/auth/github` | The GitHub OAuth login flow needs a real GitHub App and can't be exercised locally without one; since `requireAuth` only verifies the JWT signature and reads `sub`/`username`/`platformRole` out of it (no DB session lookup), a correctly-signed token is indistinguishable from one issued by a real login for testing purposes | Why is it safe to test the endpoint with a hand-crafted token instead of a real login? |
| 2026-07-30 | A-14 | Implemented `GET /api/repos/:repoId/debt` — added `getRepoDebt` to `repoService.ts` and wired the route in `routes/repos.ts`, following the same thin route → service function → `AppError` pattern as `/trend` | Keeps the second `/api/repos/*` endpoint consistent with the first instead of introducing a new structure two endpoints in | Why does this endpoint reuse `getActiveRepo` instead of writing its own repo-existence check? |
| 2026-07-30 | A-14 | Scoped the route with `requireAuth` + `requireRepoAccess('read')` rather than matching `/trend`'s auth-only check, even though the spec lists "Auth: Required" identically for both | Debt figures are closer to `/members`-level sensitivity than a public trend line, and the spec being silent on membership isn't the same as the spec ruling it out; flagged the inconsistency with `/trend` rather than silently copying it | Should `/trend` be tightened to match, given it has the same gap? |
| 2026-07-30 | A-14 | Return `404 NOT_FOUND` when a repo has zero `HealthSnapshot` rows, instead of a `200` with an all-zero breakdown | Neither `api_design.md` nor `database_design.md` specifies this case; treating "never analyzed" as an error keeps it distinguishable from "analyzed and found zero debt," which a zeroed `200` would hide | How would a caller tell "no analysis yet" apart from "genuinely zero debt" if this returned `200`? |
| 2026-07-30 | A-14 | Aggregated `Finding` rows with `groupBy({ by: ['category'], where: { snapshotId } })`, filtering on `snapshotId` rather than `repoId`, then zero-filled all 5 categories before overlaying the grouped results | Debt breakdown is a snapshot-in-time view; filtering on `repoId` alone would sum findings across every historical snapshot for that repo and overcount. Zero-filling first guarantees categories with no findings (e.g. `maintainability`) still appear, matching the `api_design.md` example response | What would the response look like for a category with no findings if the zero-fill step were skipped? |
| 2026-07-30 | A-14 | Mapped the Prisma `FindingCategory` enum to the response's lowercase snake_case keys with an explicit `Record` lookup instead of `.toLowerCase()`, even though both produce the same string today | An explicit map is the honest source of truth for which 5 categories exist in the API contract; a new enum value added later would be silently included by `.toLowerCase()` with a guessed key, whereas the explicit map would leave it out until someone updates it on purpose | Since `.toLowerCase()` produces identical output today, why not use it and save a few lines? |
| 2026-07-30 | A-14 | Verified all six cases (`401`, `403` non-member, `200` owner, `200` member, `404` unknown repo, `404` zero-snapshot repo) against real seed data, cross-checking the `200` response numbers against the raw `HealthSnapshot`/`Finding` rows rather than just checking the status code | A `200` with the wrong numbers is a worse bug than a wrong status code and wouldn't be caught by status-only testing; the zero-snapshot case needed a second, temporary repo row created and deleted specifically for that test since the seed only ever creates one analyzed repo | Why wasn't checking for `200 OK` enough to call the aggregation correct? |
| 2026-08-03 | E-01 | Scaffolded `apps/mobile` with `create-expo-app` (blank TypeScript template) on Expo SDK 57, added `react-native-web`/`react-dom` for browser preview, and confirmed npm workspaces picks it up cleanly from the repo root with no duplicate `node_modules` | `project_proposal.md` names Expo v51, but that line predates the current SDK by several major versions and isn't one of the locked stack decisions in `docs/CLAUDE.md` — SDK 57 is the actively supported version, so scaffolding onto an already-outdated SDK would just create an upgrade task later for no benefit now | Why scaffold on SDK 57 instead of the SDK 51 named in the proposal doc? |
| 2026-08-03 | E-01 | Deleted `LICENSE`, `AGENTS.md`, `CLAUDE.md`, and `.claude/settings.json` that `create-expo-app` had generated inside `apps/mobile` before committing | The template ships its own MIT license file (Expo's copyright, not this project's) and a `.claude/settings.json` that silently enables an unrelated Claude Code plugin; neither is something this task asked for, and committing them would be an unreviewed tooling change riding along with the scaffold | What would have happened if the `.claude/settings.json` file had been committed as-is? |
| 2026-08-03 | E-01 | Verified the scaffold by running `npx expo start` and opening the app in-browser (`w`), rather than trusting a clean `npm install` alone as proof it worked | A dependency install finishing without errors only proves the packages resolved, not that the app actually boots and renders; Metro bundling and a rendered "Open up App.tsx" screen is the actual acceptance criterion | Why wasn't a successful `npm install` enough to call the scaffold done? |
| 2026-08-03 | E-02 | Added `@react-navigation/native` + `@react-navigation/bottom-tabs` (with `react-native-screens`/`react-native-safe-area-context` peer deps) instead of switching the project to Expo Router | `apps/mobile` still uses the plain `App.tsx`/`index.ts` entry from E-01; Expo Router needs a file-based `app/` directory restructure, a bigger change than a scaffolding step justifies, whereas React Navigation drops into the existing entry point with explicit, code-visible routing | Why not use Expo Router since it ships with newer Expo templates? |
| 2026-08-03 | E-02 | Scaffolded only two tabs — Home and Notifications — in `navigation/TabNavigator.tsx`, not a third Profile tab | Those are the only two mobile screens with an actual WBS task behind them (`E-05` home, `E-07` notifications); a Profile tab has no assigned task yet, so adding it now would be a speculative screen nobody asked for | Why not add a Profile tab now to avoid restructuring the navigator later? |
| 2026-08-03 | E-02 | Created `screens/HomeScreen.tsx` and `screens/NotificationsScreen.tsx` as empty, identically-shaped placeholders (centered `Text` in a `View`), and replaced `App.tsx`'s entire placeholder content with `<TabNavigator />` | This step's acceptance criteria is scaffolding only, no real data; keeping both screens structurally identical signals to a reviewer they're matching stubs, and the old `App.tsx` "Open up App.tsx" content was fully superseded once the navigator became the app's root | Why replace all of `App.tsx` instead of keeping its old content alongside the navigator? |
| 2026-08-03 | E-02 | Verified by running the app with `npm run web --workspace=apps/mobile` and confirming the bottom tab bar renders both tabs and switches screens on tap, not just that the TypeScript compiled | A clean compile only proves the navigation types line up; the actual acceptance criterion is that `NavigationContainer`/`createBottomTabNavigator` actually mount and respond to taps at runtime | What could compile cleanly here but still fail to render a working tab bar? |
| 2026-08-05 | A-15 | Implemented `GET /api/repos/:repoId/hotspots` — added `getRepoHotspots` to `repoService.ts` and wired the route in `routes/repos.ts` with the same `requireAuth` + `requireRepoAccess('read')` guard and thin route → service pattern as `/debt` and `/trend` | Ranks files by aggregating `Finding` with `groupBy(['file'])` ordered by `_count` desc, then runs a second `groupBy(['file','severity','state'])` scoped to only the top-ranked files for the per-severity/new-count breakdown, reusing the `[snapshotId, file]` index that `database_design.md` had already reserved for this query but that nothing queried until now | Why run two separate `groupBy` queries instead of one? |
| 2026-08-05 | A-15 | Excluded `Finding` rows with `file: null` from the ranking entirely, capped `limit` at 100 (default 10, `400` outside that range), and treated a `snapshotId` query param belonging to a different repo as `404` rather than leaking that the snapshot exists elsewhere | `api_design.md` specifies the response shape but leaves these three edges undefined; a null file can't be attributed to a "worst file", an uncapped `limit` would let a client force an unbounded scan, and a distinguishable error for a cross-repo snapshot ID would be a tenant-isolation leak matching the same concern `requireRepoAccess` already guards against elsewhere in this router | What would a client be able to infer about another repo's data if the cross-repo `snapshotId` case returned a different error than "not found"? |
| 2026-08-05 | A-15 | Confirmed with `tsc --noEmit` only — have not yet exercised the endpoint against real seed data or checked the four response cases (`200` ranked correctly, `limit` clamping, cross-repo `snapshotId` `404`, zero-snapshot repo `404`) the way `/debt` and `/trend` were verified | Type-checking proves the Prisma query shapes compile against the schema, not that the ranking or zero-filled `bySeverity` output is actually correct against live data; this is a known gap, not a finished verification pass | What could type-check cleanly here but still return the wrong ranking or wrong `bySeverity` counts at runtime? |
| 2026-08-06 | A-35 | Added `lib/oauthStateStore.ts` (`consumeStateNonce`, a Redis `SET key val EX 600 NX`) and called it from `handleGithubCallback` right after `verifyState` succeeds, rejecting a second use of the same `state` with `400 INVALID_STATE` | The existing `state` was a signed JWT with a random `nonce`, but `verifyState` only checked the signature and expiry — nothing recorded that a nonce had been used, so a captured `state` value could be replayed for its whole 10-minute window; `SET NX` is atomic, so two requests racing on the same stolen value can't both pass | Why does the nonce need to be marked used in Redis instead of just checking the JWT signature and expiry? |
| 2026-08-06 | A-35 | Left the Redis call unguarded by a try/catch, so a Redis outage makes login fail with a `500` instead of silently letting logins through | The API already treats Redis as required infrastructure for the analysis queue; if this call failed open instead, the one time Redis is down the replay check would silently disable itself, which is a worse failure mode than login being briefly unavailable | Why not catch the Redis error and let login continue if Redis is unreachable? |
| 2026-08-06 | A-35 | Verified `consumeStateNonce` against real local Redis with a throwaway script calling it twice with the same string (`true` then `false`), then deleted the script; did not run the change through a real GitHub OAuth login | The isolated call proves the atomic replay check itself works against the real Redis instance; a full browser login was judged unnecessary since the change only adds one check after the existing `verifyState` call, is fully typed (`tsc --noEmit` passed), and would fail loudly on the very next real login if the wiring were wrong | What would happen on the next real login if `statePayload.nonce` were undefined for some reason? |
| 2026-08-06 | A-16 | Implemented `GET /api/repos/:repoId/pulls/:prNumber` — added `getRepoPullRequestDetail` to `repoService.ts` and wired the route in `routes/repos.ts`, same `requireAuth` + `requireRepoAccess('read')` guard and thin route → service pattern as `/debt`, `/hotspots`, and `/trend` | The `/pulls` list half of A-16 already existed (written earlier for the dashboard integration pass); only the single-PR detail endpoint was missing, so this added just that piece rather than rebuilding both | Why does this endpoint fetch every `AnalysisJob`/`HealthSnapshot` for the PR instead of just the latest one like the list endpoint does? |
| 2026-08-06 | A-16 | Left the existing `/pulls` list endpoint's response shape untouched even though it diverges from `api_design.md` (`id/author/branch/score` instead of `prNumber/authorLogin/headBranch/latestScore`, no pagination); documented the drift as a flag in `api_design.md` next to the spec instead of silently fixing it | Renaming an already-shipped endpoint's fields is a breaking change for whatever already calls it (`apps/web`), and isn't part of what A-16 was actually missing; a written flag next to the spec keeps the mismatch visible for whoever picks it up without bundling an unrelated risky change into this task | What could break if the list endpoint's field names were renamed to match the doc without checking `apps/web` first? |
| 2026-08-06 | A-16 | Added `Number.isInteger(prNumber)` validation in the route before calling the service, after discovering `GET /pulls/abc` returned a raw `500 INTERNAL_ERROR` instead of a clean `400` | `Number("abc")` is `NaN`, and passing `NaN` into `prisma.pullRequest.findUnique`'s `Int` filter throws a Prisma error rather than returning "not found," so it fell through to the generic error handler instead of a proper 4xx; validating in the route catches this before it reaches Prisma | Why does an invalid `prNumber` need explicit validation instead of letting Prisma's `findUnique` return `null` for it like a nonexistent-but-valid PR number does? |
| 2026-08-06 | A-16 | Verified all four cases against real seed data: `200` with correct `snapshots`/`newIssues` for a real PR, `404` for an unknown `prNumber`, `400` for a non-numeric `prNumber`, and `404` (repo not found, not PR data) for `seed-outsider` — a real `globex`-org user with no access to the `acme`-owned test repo — using a hand-signed JWT rather than the `demo-token` shortcut | The `demo-token` user is actually an `OWNER` of the seeded `acme` org, so testing only with it would never have exercised `requireRepoAccess`'s rejection path; `seed-outsider` exists in the seed specifically to prove cross-tenant isolation, so it was the only way to confirm a non-member is genuinely blocked rather than assumed blocked | Why wasn't testing with `demo-token` alone sufficient to call access control verified? |
| 48 | 2026-08-06 | A-17 | Implemented `GET /api/snapshots/:snapshotId/findings` — new `snapshotService.ts` and `routes/snapshots.ts`, mounted in `app.ts`, instead of adding it to `repoService.ts`/`repos.ts` | This route is keyed by `snapshotId`, not `repoId`, so it doesn't belong under the existing repo router; giving it its own service/route file matches how `notifications`/`qualityGates` each already got a dedicated file rather than being folded into an unrelated one | Why not just add one more function to the existing `repoService.ts`? |
| 49 | 2026-08-06 | A-17 | Since there's no `repoId` route param here, `requireRepoAccess` middleware can't be reused as-is; copied its org/role check inline into `snapshotService.ts` (keyed off the snapshot's own `repoId`) instead of extracting that logic out of `requireRepoAccess.ts` into a shared function | The extraction would touch a shared middleware file for a one-off task, and the duplicated block is ~15 fixed lines unlikely to drift; a third call site needing the same check would be the actual trigger to extract it, not a second one | What happens if `requireRepoAccess`'s rule set changes later — does this copy silently go stale? |
| 50 | 2026-08-06 | A-17 | Found and fixed a bug before shipping: computing `newCount` as `{...where, state: 'NEW'}` when `where` already had `state: 'EXISTING'` baked in from an `isNew=false` filter — the later key silently overwrote the filter, so a live test returned `"new": 2, "carryOver": -1` on a 1-row filtered result | Split the query into `summaryWhere` (category/severity/file only) and `dataWhere` (`summaryWhere` plus the `isNew`-derived `state`), so `summary` always reflects a stable new/carryOver breakdown independent of which `isNew` slice `data`/`pagination` are currently showing | Why should toggling `isNew` change which rows come back in `data` but not change the `summary` counts? |
| 51 | 2026-08-06 | A-17 | Verified `200` (unfiltered + each filter), `400` (bad `severity`), `401` (no token), and `404` (unknown snapshot, and `seed-outsider` hitting a snapshot in an org they're not in) against real seed data; did not exercise the `403` branch (org member with no repo access) since no seed user matches that shape | The `403` path is the same inline copy of `requireRepoAccess`'s already-tested role logic used in `/pulls/:prNumber` and friends, so it's covered by that precedent rather than by fabricating a seed row just to force a status code | Would it be worth adding a seed user in that exact "org member, no repo access" shape so this branch stops being untested? |
| 2026-08-08 | E-03 | Built the mobile login flow in `AuthContext.tsx` — `WebBrowser.openAuthSessionAsync` opens `GET /auth/github?redirect=codehealth://auth`, waits for the browser to hit that scheme, reads `token` off the returned URL, validates it with `GET /auth/me`, then persists it with `SecureStore.setItemAsync`; `App.tsx` gates the tab navigator behind `isAuthenticated` and shows `LoginScreen.tsx` otherwise | GitHub OAuth's registered `redirect_uri` is a fixed HTTPS URL on the API, not something the app can register per-client, so the app can't get a direct GitHub redirect back to itself; going through the system browser with a scheme the API can redirect to afterward avoids putting the OAuth client secret in the mobile bundle, which a token-exchange-in-app approach would have required | Why does the app open a system browser instead of exchanging the code for a token itself? |
| 2026-08-08 | E-03 | Registered `"scheme": "codehealth"` in `app.json` and added `expo-secure-store`, `expo-web-browser`, `expo-linking` via `npx expo install` rather than hand-picking versions | `expo install` resolves each package against the exact SDK 57 version already locked for this project, and it auto-registered the `expo-secure-store`/`expo-web-browser` config plugins in `app.json`, which a manual `npm install` would have left for me to add by hand and risk getting wrong | What could go wrong from installing these with plain `npm install` instead of `expo install`? |
| 2026-08-08 | E-03 | Specified but did not implement two changes needed in `apps/api` — allowlisting `codehealth://auth` in `sanitizeRedirect` (`authService.ts`) and branching `GET /auth/github/callback` to a `302` with `?token=` when `state` carries that redirect, versus the existing `200` JSON response the web app still relies on — and handed the exact diff to Rumesh instead of writing it myself | `apps/api` is Rumesh's app per the ownership rules, and GitHub's registered `redirect_uri` (`GITHUB_OAUTH_CALLBACK_URL`) points straight at his callback route; only that route holds the client secret needed to exchange a code for a token, so there's no way to finish this OAuth leg from the mobile side alone | Why can't the token exchange happen entirely on the mobile side without a backend change? |
| 2026-08-08 | E-03 | Confirmed `tsc --noEmit` passes for `apps/mobile`; have not run the real GitHub round trip, SecureStore persistence across an app restart, or the expired-token fallback to the login screen, since all three need the backend redirect change above to actually reach `codehealth://auth` with a token | A clean type-check only proves the SecureStore/fetch/WebBrowser calls are shaped correctly, not that GitHub actually redirects back into the app or that a restored session survives a real app relaunch; task is implementation-complete but not verification-complete until Rumesh's side lands | What could type-check cleanly here but still fail the first time someone actually taps "Continue with GitHub"? |
| 2026-08-16 | E-03 | Implemented the `apps/api` backend piece specified on 2026-08-08 — widened `sanitizeRedirect` in `authService.ts` to also accept the `codehealth://` scheme, threaded `statePayload.redirect` through `handleGithubCallback`'s return value, and changed `GET /auth/github/callback` in `routes/auth.ts` to `res.redirect()` with `?token=` appended instead of returning `200` JSON | This is the exact diff already handed to Rumesh as a spec on 2026-08-08 rather than written directly, since `apps/api` is his app per the ownership rules; wrote it directly this time at his request instead of handing over another diff | Why was this written directly in `apps/api` this time instead of handed to Rumesh as a diff like on 2026-08-08? |
| 2026-08-16 | E-03 | Confirmed `tsc --noEmit` passes for `apps/api` after the change; have not run the real GitHub OAuth round trip or confirmed `WebBrowser.openAuthSessionAsync` actually resolves `type: 'success'` against a live login, and no automated test exists yet for the callback route's redirect behavior | Same gap as the 2026-08-08 mobile-side entry — a clean type-check proves the redirect URL is shaped correctly, not that GitHub's browser flow actually lands back in the app with a token; task is implementation-complete but not verification-complete | What could type-check cleanly here but still fail the first time someone taps "Continue with GitHub" on a real device? |
| 2026-08-17 | E-03 | Added `apps/mobile/lib/apiClient.ts` and `authTokenStore.ts` (axios instance, request/response interceptors, single-flight refresh, SecureStore-backed refresh token) and rewrote `AuthContext.tsx` to go through them instead of raw `fetch` calls — login, logout, and the mount-time session restore all route through the new client now | `apps/web` already has this exact pattern proven out (`apiClient.ts`/`authTokenStore.ts` with interceptor-based refresh), so porting it instead of inventing a mobile-specific approach keeps both clients consistent, with the cookie-based refresh swapped for SecureStore since mobile has no cookie jar | Why keep the in-memory access token separate from the SecureStore-persisted refresh token instead of storing both the same way? |
| 2026-08-17 | E-03 | Running an actual login on a real device surfaced two bugs `tsc --noEmit` couldn't have caught: `login()` never sent `?client=native`, so the backend silently took the web/cookie branch instead; and `sanitizeRedirect` in `authService.ts` only kept strings starting with `/`, dropping the `exp://.../--/auth` deep link before the 2026-08-16 redirect fix ever had a target to redirect to. Fixed both | Allowlisted `codehealth://` and `exp://` explicitly in `sanitizeRedirect` rather than accepting any absolute URL, so the existing "otherwise this is an open redirect" comment on that function stays true — only the app's own scheme and the Expo dev-client scheme are new, not arbitrary hosts | Why does `sanitizeRedirect` need to allow `exp://` as well as `codehealth://`, and not just the app's real scheme? |
| 2026-08-17 | E-03 | Verified end-to-end on a real Android device through Expo Go: full login round trip (GitHub authorize → callback redirect → deep link back into the app → `/auth/me`), the browser sheet closing itself instead of hanging open, and session restore after a full force-quit/relaunch via the SecureStore-persisted refresh token. Closes the verification gap the 2026-08-16 entry left open | Neither bug in the row above would have shown up in a type-check — both only surfaced once a browser actually followed the real redirect chain on a device, which is why a live run was the actual bar here, not `tsc --noEmit` passing | What would have shipped broken if this had been called done on `tsc --noEmit` alone, the way the 2026-08-16 entry flagged as the known gap? |
| 2026-08-18 | D-11 | Extended `RepositoryOverviewPage.tsx`'s debt fetch to keep the `breakdown` field the `/debt` endpoint already returned, and added a `Pie`/`Cell` donut chart plus a legend under a new "Debt Breakdown" card, reusing the existing `ResponsiveContainer`/`Tooltip` styling from the page's health-trend area chart | The `breakdown` object (5 categories, zero-filled, count + debtMinutes each) was already being fetched and discarded — this only added a consumer for data the A-14 endpoint had shaped for exactly this chart | Why does the center-of-donut total use `totalDebtMinutes` from the snapshot instead of summing the breakdown values shown in the legend? |
| 2026-08-18 | D-11 | Colored 4 of the 5 slices with the existing `--destructive`/`--info`/`--warning`/`--success` theme tokens, but hardcoded Code Smell to a fixed violet (`#a78bfa`) instead of `--primary` | `--primary` (162°) and `--success` (145°) sit close enough in hue that their legend dots read as the same green in the dark theme, which would have made a category with real debt minutes (Code Smell) visually indistinguishable from one that was often zero (Maintainability) | Why not add a sixth theme token instead of hardcoding one chart-only color? |
| 2026-08-18 | D-11 | Verified against real seeded data (`demo_developer` dev-login, `seed-acme/code-health-demo` repo) rather than just `tsc --noEmit` — confirmed the donut renders only the 3 nonzero categories, the legend minutes match the seeded `Finding` rows exactly, and the zero-value categories still appear in the legend without a visible slice | A clean type-check would have proven the `breakdown` shape lined up with the extended type, not that Recharts actually draws a correct proportional donut or that the zero-fill from A-14 renders sensibly with no slice instead of erroring | What would a nonzero-but-untested category (e.g. Duplication) have looked like on the chart if the zero-fill from A-14 had been broken? |
| 2026-08-20 | D-12 | Built `HotspotTable.tsx` — a client-side sortable table (rank, file, total issues, new issues, debt) consuming the already-implemented `GET /api/repos/:repoId/hotspots` (A-15) — and wired it into `RepositoryOverviewPage.tsx` as a new "Hotspots" card, with row-click navigating to `/repositories/:repoId/findings?file=...` | The backend ranks by `totalFindings` desc and takes no sort-column query param, so per-column sorting has to happen client-side on the fetched array rather than by re-fetching; reused the existing `DataTable`/`EmptyState` primitives from `components/ui` instead of a one-off table markup, matching every other list on the page | Why does sorting re-derive the rank column instead of trusting a `rank` field from the API response? |
| 2026-08-20 | D-12 | While wiring this up, discovered the "Debt Breakdown" donut chart from D-11 (commit `12e870f`, merged via PR #72) had been silently dropped by a later merge commit (`ac358d1`, merging `develop` into `feature/nethmi-frontend-ui`) that resolved a conflict on `RepositoryOverviewPage.tsx` by keeping the redesigned-UI branch's version of the file wholesale, discarding the donut JSX while leaving its now-unused `Pie`/`PieChart`/`Cell` imports behind as the only trace; ported the original JSX back in, adapted to the current `Card`-based layout | A merge conflict resolved by picking one side's entire file instead of combining both sides' independent changes silently deletes whichever side didn't win — `git log`/`git show` on the commit that touched the file right after D-11 landed was what surfaced it, not a runtime symptom | How would this have been caught earlier, before it needed a git-archaeology session to notice? |
| 2026-08-20 | D-12 | Found and fixed an unrelated pre-existing bug blocking end-to-end verification: `RepositoriesPage.tsx`'s `fetchRepos` was a plain function re-created every render but listed as its own `useEffect` dependency, causing an infinite fetch loop that looked like a permanently-stuck "Loading repositories from Postgres…" spinner rather than a crash; wrapped it in `useCallback` keyed on `selectedOrg` | The loop wasn't visible from the code alone without actually running the page — `tsc --noEmit` has no opinion on `useEffect` dependency correctness, only real interaction (or the Network tab showing repeated `xhr` requests) exposed it | Why didn't a clean `tsc --noEmit` catch a bug this disruptive? |
| 2026-08-20 | D-12 | Verified against real seeded data end-to-end (not just `tsc --noEmit`): fixed `DATABASE_URL` port mismatches in `apps/api/.env` and `packages/db/.env` (pointed at `5432`, `docker-compose.yml` maps Postgres to `5433`), ran `prisma migrate dev` + `prisma db seed`, logged in via `/auth/dev-login` as the seeded `demo_developer` (owner of `seed-acme`), and confirmed the Hotspots card renders the 3 seeded findings ranked correctly | The `.env.example` files still document port `5432` as the default, which doesn't match this project's `docker-compose.yml` mapping — worth fixing in `.env.example` itself so the next person setting up local dev doesn't hit the same silently-hanging Postgres connection | Should `docker-compose.yml` be changed to map Postgres to the standard `5432` instead, so `.env.example`'s default is correct without editing it? |
| 2026-09-01 | A-22 | Implemented `GET /api/mobile/summary` and `GET /api/mobile/repos/:repoId/smells` — new `mobileService.ts` (`getMobileSummary`, `getRepoSmells`) and `routes/mobile.ts`, mounted in `app.ts`; exported the previously-private `getActiveRepo` from `repoService.ts` so the new service could reuse the same repo-existence guard instead of duplicating it | Following the same thin route → service function → `AppError` pattern as every other repo-scoped endpoint kept these two mobile endpoints consistent with the rest of the API instead of inventing new structure for the first mobile route file | Why does `/smells` reuse `getActiveRepo` instead of writing its own check? |
| 2026-09-01 | A-22 | Decided the smells endpoint returns every `Finding` row for the latest snapshot, not just `category: CODE_SMELL`, after noticing `api_design.md`'s own example response includes a `rule: "complexity"` finding — that's `FindingCategory.COMPLEXITY`, not `CODE_SMELL` — inside the `smells` list | Filtering strictly to `CODE_SMELL` would have contradicted the doc's own example response; treating "smells" as the mobile-card umbrella term for any finding matches what the spec actually shows, not just what the endpoint's name implies | What would the response look like for a repo whose only issues are complexity/duplication findings if this had filtered by `category: CODE_SMELL`? |
| 2026-09-01 | A-22 | Computed `scoreChange` as `latest.healthScore - previous.healthScore` from the two most recent `HealthSnapshot` rows, defaulting to `0` when only one snapshot exists, since no `scoreChange`/`healthScoreChange` field exists anywhere in the schema | `api_design.md`'s example shows a nonzero `scoreChange` but doesn't define how it's derived; comparing the two latest snapshots directly keeps the number honest to what's actually stored, rather than adding a persisted delta column to `HealthSnapshot` for one field this task didn't ask for | Why not add a persisted `scoreChange` column to `HealthSnapshot` instead of computing it on every request? |
| 2026-09-01 | A-22 | Verified both endpoints against real seed data (dev-login as `seed-admin`) rather than just `tsc --noEmit`: confirmed `/summary`'s repo list, `healthScore`, and `openPRs` matched the seeded `code-health-demo` repo, and `/smells` returned the 3 seeded findings sorted worst-severity-first with `totalSmells`/`newSmells` matching the raw `Finding` counts; also confirmed `limit=abc`, `limit=101`, and a nonexistent `repoId` all return the correct `400`/`404` error shapes | A clean type-check only proves the Prisma query shapes compile, not that the aggregation, ordering, or validation boundaries are actually correct against live data — same gap flagged in earlier hotspots/debt log entries | What would `scoreChange` have shown if the two latest seeded snapshots hadn't happened to land on the same capped `healthScore`? |