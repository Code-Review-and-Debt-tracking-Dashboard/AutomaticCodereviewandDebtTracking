| Date | Task ID | What it does  | Why this approach  | Question I'd get asked |
|---|---|---|---|---|
| 2026-07-16 | A-03 | GitHub Actions workflow runs lint+typecheck on push/PR to main & develop | Followed the CI shape already in system_architecture.md §5.6 instead of inventing one; used `--if-present` so it's a safe no-op until apps add real scripts | Why does it pass with no actual linting happening yet? |
| 2026-07-16 | A-03 | Replaced the bash if/else guard with `--include-workspace-root` flag on the lint/typecheck commands | Native npm flag makes root a fallback target, so `--workspaces --if-present` never errors on zero-workspaces — no shell logic needed, and no dead guard to remove later | Why is this safer than the bash guard I had before? |
| 2026-07-18 | A-04 | `apps/api` entry point (`src/index.ts`) + middleware stack (`helmet` → `cors` → `express.json()`) in `src/app.ts`, exported via a `createApp()` factory | Matches the middleware layer in system_architecture.md §3.1, but scoped to only what A-04 asks for — rate limiting (A-26), auth (A-06), and Pino logging (A-29) are separate dependent tasks so I left them out instead of building them early | Why doesn't the scaffold have rate limiting or auth yet if the architecture diagram shows them in the same layer? |
| 2026-07-18 | A-04 | Global error handler as an `AppError` class + `notFoundHandler` + `errorHandler`, registered last in `app.ts` | `errorHandler` takes 4 params `(err, req, res, next)` — that's how Express distinguishes it from regular middleware, and it only catches errors from routes registered *before* it, so it (and `notFoundHandler`) must stay last even as later tasks add real routes. Response shape follows api_design.md §10 exactly so every endpoint returns the same error JSON | How does Express know `errorHandler` is an error handler and not a normal middleware function? |
| 2026-07-22 | C-01 | Reviewed the schema and removed ~18 redundant single-column indexes (leftmost-prefix duplicates of composite/unique indexes) and the duplicate `HealthSnapshot.createdAt` | Each redundant index still costs write amplification and storage on every insert/update but is never chosen by the planner when a composite/unique index already covers its leftmost column. `calculatedAt` already captured what `createdAt` duplicated | Why remove indexes — don't more indexes always make queries faster? |
| 2026-07-22 | C-02 | Folded the index-pruning changes into the original `init` migration instead of shipping a second "drop indexes" migration | No database had applied the old `init` yet (fresh project, no deployed DB), so squashing before first deploy is standard and leaves a clean baseline. Avoids a confusing "created then dropped" trail in the migration history for evaluators | Why is it safe to edit the init migration instead of adding a new one? |
| 2026-07-22 | C-08 | Adopted a dual-role model — `PlatformRole` on `User` (platform-wide) + `RepositoryRole`/`MemberStatus` on `RepositoryMember` (per-repo) — over a single global `role` | A single global role can't express "admin on repo X, viewer on repo Y" in a multi-repo product. Splitting platform-level from repository-level authorization is the semantically correct model and matches how access is actually checked in the API | Why two role fields instead of one — isn't that over-engineered for a student project? |
| 2026-07-22 | C-01 | Split `AnalysisJob` (mutable run lifecycle: status, trigger, `bullJobId`) from `HealthSnapshot` (immutable result: scores, `gateResult`, `calculatedAt`), 1:1 | Mixing a mutable status machine with the immutable historical record makes trend queries fragile and invites accidental mutation of past results. Separating them keeps snapshots append-only, which is what the trend chart depends on | Why not just put the status and the results on one table? |
| 2026-07-22 | C-01 | Kept GitHub OAuth tokens in a separate `GitHubCredential` table and stored only a **hash** of session tokens | Isolating secrets limits blast radius if `User` rows leak, and hashing sessions means a DB dump can't be replayed as valid sessions. The worker receives `analysisId`/`repoId` via the BullMQ payload and fetches the token itself rather than passing raw tokens through the queue | Why not store the access token directly on the User row? |
| 2026-07-23 | A-05 | `/auth/github` redirects to GitHub's consent screen carrying a signed, short-lived `state` (JWT with a nonce + optional `redirect`); `/auth/github/callback` verifies `state`, exchanges `code`, and returns `{token, user}` | The API is stateless — there's no session store to put CSRF `state` in before the user is even logged in — so signing it with `JWT_SECRET` (10 min expiry) lets the callback detect tampering/replay without extra infra. `redirect` is only accepted if it starts with `/`, so a crafted `?redirect=http://evil.com` is silently dropped instead of causing an open redirect | Why not just store `state` in a cookie or a `Session` row? |
| 2026-07-23 | A-05 | GitHub access token is AES-256-GCM encrypted (`lib/crypto.ts`) before being written to `GitHubCredential.encryptedAccessToken`, stored as `iv:authTag:ciphertext` | database_design.md §3.10 requires the token be encrypted at rest; GCM's `authTag` gives tamper-detection for free, so a modified ciphertext fails to decrypt loudly instead of returning silently-wrong bytes | What happens if someone edits the encrypted value directly in the DB? |
| 2026-07-23 | A-05 (touches C-03) | Added a minimal `@codehealth/db` re-export (`packages/db/index.ts`) exporting one shared `PrismaClient` singleton, since A-05 is the first task that actually persists anything | C-03 (the real client package task, owned by a teammate) wasn't done yet and A-05 can't upsert a `User` without a usable client. Kept it to the smallest possible slice — one singleton + re-exported types, nothing else — so it doesn't preempt the fuller repository-layer work C-03 still owns | Isn't touching packages/db stepping on a teammate's task? |
| 2026-07-23 | A-05 | Constructed `PrismaClient` with an explicit `@prisma/adapter-pg` driver adapter (`new PrismaClient({ adapter })`) instead of the bare constructor | Prisma 7's client engine ships with no bundled query-engine binary — `new PrismaClient()` throws `PrismaClientInitializationError` immediately unless a driver adapter is passed. This wasn't visible from the schema: Prisma 7 moved the connection URL out of `datasource db { url = ... }` and into `prisma.config.ts` (CLI-only), so the generated client needs the connection string handed to it again at runtime via the adapter | Why did the server crash on startup before any request even hit a route? |
| 2026-07-23 | A-06 | `requireAuth` middleware verifies the `Authorization: Bearer <jwt>` header with `jwt.verify` and attaches `{ id, username, platformRole }` to `req.user` via a global `Express.Request` type augmentation | Kept it fully stateless — no DB or Redis lookup per request — to match the "stateless JWT only" note already left in `jwt.ts`; Redis (A-09) isn't wired up until next sprint, so a session-store check wasn't an option yet anyway | Why doesn't the auth guard check the database on every request to confirm the user still exists? |
| 2026-07-23 | A-06 | `POST /auth/logout` just returns `204` with no server-side state change; `GET /auth/me` re-fetches the user row from the DB instead of trusting the JWT payload | A stateless JWT can't be truly revoked without a token denylist, which needs Redis — out of scope until A-09, so logout is client-side-only for now (client discards the token). `/auth/me` still needs the DB regardless, since `createdAt` isn't in the JWT payload, and it doubles as a check that catches a deleted user whose old token would otherwise still pass `requireAuth` | If logout doesn't invalidate anything server-side, what actually stops a token from being reused right after "logging out"? |
| 2026-07-23 | A-02 | Root `docker-compose.yml` for local Postgres 16 + Redis 7, credentials hardcoded as plain `environment:` values matching `packages/db/.env.example`'s documented default (`postgres`/`postgres`) | For local-dev-only infra, hardcoding throwaway credentials in a git-tracked compose file is standard practice (same pattern Postgres's own docs use) — the real deployed DB (Supabase/Railway) is configured separately through the platform's own env var UI, so there's no actual secret in this file to leak | Why is a hardcoded password in a committed file OK here but not in production? |
| 2026-07-24 | A-07 | `POST /webhooks/github` is mounted *before* the global `express.json()` in `app.ts`, using its own `express.raw({ type: 'application/json' })` so `req.body` stays the exact `Buffer` GitHub signed | If the JSON parser ran first it would consume the stream and hand the route an already-parsed object; re-serializing that back to a string before hashing isn't guaranteed to byte-match what GitHub originally sent (key order, whitespace), so the HMAC would fail unpredictably. Only the raw bytes are provably identical | Why not just parse the JSON first and verify the signature after? |
| 2026-07-24 | A-07 | `verifyWebhookSignature` compares the computed `sha256=<hmac>` to the `X-Hub-Signature-256` header with `crypto.timingSafeEqual`, not `===` | A plain string/`===` comparison exits on the first mismatched byte, so response time leaks how many leading bytes of a forged signature were already correct. `timingSafeEqual` always takes the same time regardless of where the mismatch is — this is the exact mitigation GitHub's own webhook security docs call for | Why does a webhook signature need constant-time comparison — isn't the secret already unguessable? |
| 2026-07-24 | A-07 | On a valid signature the handler currently returns a placeholder `202 { message: "Received" }` instead of api_design.md §3's full `{ message: "Job queued", jobId }` shape | A-07 in the WBS is scoped to receive + verify only — parsing the PR payload out of the body is A-08 and enqueueing the BullMQ job is A-10, both separate tasks with their own dependencies that don't exist yet. Returning the final shape now would mean fabricating a `jobId` | Why doesn't the response match the full api_design.md contract yet? |
| 2026-07-24 | A-08 | `webhookService.ts` parses the raw `Buffer` body into the exact extract shape from api_design.md §3 (`prNumber`, `headSha`, `githubRepoId`, …) and looks up the linked repo by `githubRepoId`; the route only does HTTP branching | Keeps the route thin (same pattern as `authService.ts`) and makes the parse/lookup pure enough to unit-test later without spinning up Express. `githubRepoId` is stringified because Prisma stores it as `String`, while GitHub sends a number | Why put parsing in a service instead of doing `JSON.parse` inside the route handler? |
| 2026-07-24 | A-08 | `ping` → `200 pong`; unsubscribed `pull_request` actions (e.g. `edited`) → `200` no-op; any other event type → `400 VALIDATION_ERROR` | api_design.md §3 says "400 unsupported event", but GitHub always fires a `ping` when the webhook is first registered (A-11) — rejecting it would mark the delivery failed and scare off evaluators during demo setup. Ignoring out-of-scope PR actions with 2xx also stops GitHub's retry storm for events we deliberately don't subscribe to | Why return 200 for `ping`/`edited` when the doc only lists 400 for unsupported events? |
| 2026-07-24 | A-08 | `findLinkedRepository` treats a missing row *or* `isActive === false` as "not linked" and throws `404 NOT_FOUND` | Unlinking (A-11) is expected to soft-delete via `isActive` rather than hard-delete the row (keeps historical snapshots). If we only checked existence, an unlinked repo would keep triggering analysis forever | Why check `isActive` instead of just `findUnique` and 404 on null? |
| 2026-07-24 | A-08 | Does **not** upsert a `PullRequest` row yet — only extracts + validates, then returns `202 { message: "Received" }` | A-08's WBS wording is "extract PR data, validate repo is linked"; persisting the PR and enqueueing the job belong to A-10 (and need BullMQ from A-09). Writing a half-persisted PR with no `AnalysisJob` would leave orphan rows for no gain | Why not save the PR to the database as soon as the webhook arrives? |
| 2026-07-27 | A-28 | `GET /health` in `apps/api` — pings Postgres with `prisma.$queryRaw`SELECT 1``, returns `200 healthy` / `503 degraded` with `checks: { database }` | Wrapped the probe in its own try/catch inside the handler instead of throwing into `AppError`/`errorHandler` — a dependency being down is the expected result this endpoint exists to report, not an unhandled error, so it needs to stay in the documented health-body shape rather than falling into the `{ error: ... }` envelope | Why doesn't a failed check throw like everywhere else in the app? |
| 2026-07-27 | A-28 | Left `checks.redis` and the worker's `/worker/health` out of this pass; API-side only for now | `apps/api` has no Redis client (that's A-09) and `apps/worker` is still just a `.gitkeep`, so implementing either would mean faking a check or scaffolding a worker ahead of schedule. `checks: { database }` today is honest about what's actually wired up; `redis` gets added once the BullMQ connection is real | Why doesn't the response match the full `checks` shape from api_design.md yet? |
| 2026-07-27 | A-29 | Shared Pino logger in `lib/logger.ts` — pretty-printed in dev (`pino-pretty` transport), raw JSON in prod, level from `LOG_LEVEL` — replacing the two `console.*` call sites (startup message, unexpected-error branch) | Matched the exact Pino config already written out in system_architecture.md §5.5.2 instead of improvising one; skipped `pino-http` since no doc names it as a dependency and A-29's scope is a logger, not per-request middleware | Why isn't every request auto-logged, if the architecture diagram lists "request logging" in the same middleware layer? |
| 2026-07-27 | A-29 | Health check's bare `catch {}` around the DB probe now logs `logger.warn({ err }, ...)` before setting `databaseOk = false`, instead of discarding the error | A silently swallowed error meant a real dependency failure showed up only as `"degraded"` with zero trail to debug it — the exact blind spot structured logging is supposed to close, so it was one line worth fixing alongside the rest of A-29 rather than leaving it | Isn't touching the health check outside A-29's task boundary? |
| 2026-07-28 | A-32 | `requireRepoAccess('read' \| 'write')` middleware: one `prisma.repository.findUnique` fetches the repo *and* the caller's own membership row (`members: { where: { userId } }`) in the same round trip, then branches on owner/admin/membership | Folds the existence check every repo route needs anyway together with database_design.md §3.7's Query 6 authorization check — same two lookups the doc specifies, one query instead of two, and it's index-only (PK + the `@@unique([userId, repoId])` B-tree) either way | Why fetch the membership row alongside the repo instead of a separate `can-access` query? |
| 2026-07-28 | A-32 | Write access (`POST`/`DELETE` on members) requires owner, `platformRole=ADMIN`, **or** an `ACTIVE` `TEAM_LEAD` member — not just owner/admin | api_design.md §2's general enforcement rule, database_design.md §3.7 ("the owner *or a TEAM_LEAD member* can grant scoped access"), and FR-5c/5d all agree `TEAM_LEAD` manages the repo like the owner; only the per-endpoint doc lines say "owner or admin" and §2 explicitly calls that shorthand for the general rule | The member-endpoint lines in the doc say "must be owner or admin" — why does a TEAM_LEAD get through? |
| 2026-07-28 | A-32 | `DELETE /api/repos/:repoId/members/:userId` sets `status: 'REMOVED', removedAt: new Date()` instead of deleting the row; a row already `REMOVED` reads as `404` | database_design.md §3.7 says `MemberStatus` + `removedAt` exist specifically "to support soft-removal and audit without deleting history" — a hard delete would throw that history away for no benefit | If the row still exists after DELETE, why does the API return 204 like it's gone? |
| 2026-07-28 | A-32 | `POST` on a user whose membership row exists but is `REMOVED` reactivates that row (`update` → `status: ACTIVE`, new `role`, `removedAt: null`) instead of inserting a second row or erroring | `@@unique([userId, repoId])` means a second insert would throw Prisma error P2002; reserving `409 CONFLICT` for a currently-`ACTIVE` member (the only case the doc actually calls a conflict) keeps a removed member re-addable rather than permanently stuck | Why does re-adding someone return 201 instead of 409 if a row for them already exists? |
