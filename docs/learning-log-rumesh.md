| Date | Task ID | What it does  | Why this approach  | Question I'd get asked |
|---|---|---|---|---|
| 2026-07-16 | A-03 | GitHub Actions workflow runs lint+typecheck on push/PR to main & develop | Followed the CI shape already in system_architecture.md §5.6 instead of inventing one; used `--if-present` so it's a safe no-op until apps add real scripts | Why does it pass with no actual linting happening yet? |
| 2026-07-16 | A-03 | Replaced the bash if/else guard with `--include-workspace-root` flag on the lint/typecheck commands | Native npm flag makes root a fallback target, so `--workspaces --if-present` never errors on zero-workspaces — no shell logic needed, and no dead guard to remove later | Why is this safer than the bash guard I had before? |
| 2026-07-18 | A-04 | `apps/api` entry point (`src/index.ts`) + middleware stack (`helmet` → `cors` → `express.json()`) in `src/app.ts`, exported via a `createApp()` factory | Matches the middleware layer in system_architecture.md §3.1, but scoped to only what A-04 asks for — rate limiting (A-26), auth (A-06), and Pino logging (A-29) are separate dependent tasks so I left them out instead of building them early | Why doesn't the scaffold have rate limiting or auth yet if the architecture diagram shows them in the same layer? |
| 2026-07-18 | A-04 | Global error handler as an `AppError` class + `notFoundHandler` + `errorHandler`, registered last in `app.ts` | `errorHandler` takes 4 params `(err, req, res, next)` — that's how Express distinguishes it from regular middleware, and it only catches errors from routes registered *before* it, so it (and `notFoundHandler`) must stay last even as later tasks add real routes. Response shape follows api_design.md §10 exactly so every endpoint returns the same error JSON | How does Express know `errorHandler` is an error handler and not a normal middleware function? |
| 2026-07-22 | C-01 | Reviewed the schema and removed ~18 redundant single-column indexes (leftmost-prefix duplicates of composite/unique indexes) and the duplicate `HealthSnapshot.createdAt` | Each redundant index still costs write amplification and storage on every insert/update but is never chosen by the planner when a composite/unique index already covers its leftmost column. `calculatedAt` already captured what `createdAt` duplicated | Why remove indexes — don't more indexes always make queries faster? |
| 2026-07-22 | C-02 | Folded the index-pruning changes into the original `init` migration instead of shipping a second "drop indexes" migration | No database had applied the old `init` yet (fresh project, no deployed DB), so squashing before first deploy is standard and leaves a clean baseline. Avoids a confusing "created then dropped" trail in the migration history for evaluators | Why is it safe to edit the init migration instead of adding a new one? |
| 2026-07-22 | C-08 | Adopted a dual-role model — `PlatformRole` on `User` (platform-wide) + `RepositoryRole`/`MemberStatus` on `RepositoryMember` (per-repo) — over a single global `role` | A single global role can't express "admin on repo X, viewer on repo Y" in a multi-repo product. Splitting platform-level from repository-level authorization is the semantically correct model and matches how access is actually checked in the API | Why two role fields instead of one — isn't that over-engineered for a student project? |
| 2026-07-22 | C-01 | Split `AnalysisJob` (mutable run lifecycle: status, trigger, `bullJobId`) from `HealthSnapshot` (immutable result: scores, `gateResult`, `calculatedAt`), 1:1 | Mixing a mutable status machine with the immutable historical record makes trend queries fragile and invites accidental mutation of past results. Separating them keeps snapshots append-only, which is what the trend chart depends on | Why not just put the status and the results on one table? |
| 2026-07-22 | C-01 | Kept GitHub OAuth tokens in a separate `GitHubCredential` table and stored only a **hash** of session tokens | Isolating secrets limits blast radius if `User` rows leak, and hashing sessions means a DB dump can't be replayed as valid sessions. The worker receives `analysisId`/`repoId` via the BullMQ payload and fetches the token itself rather than passing raw tokens through the queue | Why not store the access token directly on the User row? |
| 2026-07-23 | A-05 | `/auth/github` redirects to GitHub's consent screen carrying a signed, short-lived `state` (JWT with a nonce + optional `redirect`); `/auth/github/callback` verifies `state`, exchanges `code`, and returns `{token, user}` | The API is stateless — there's no session store to put CSRF `state` in before the user is even logged in — so signing it with `JWT_SECRET` (10 min expiry) lets the callback detect tampering/replay without extra infra. `redirect` is only accepted if it starts with `/`, so a crafted `?redirect=http://evil.com` is silently dropped instead of causing an open redirect | Why not just store `state` in a cookie or a `Session` row? |
| 2026-07-23 | A-05 | GitHub access token is AES-256-GCM encrypted (`lib/crypto.ts`) before being written to `GitHubCredential.encryptedAccessToken`, stored as `iv:authTag:ciphertext` | database_design.md §3.10 requires the token be encrypted at rest; GCM's `authTag` gives tamper-detection for free, so a modified ciphertext fails to decrypt loudly instead of returning silently-wrong bytes | What happens if someone edits the encrypted value directly in the DB? |
| 2026-07-23 | A-05 (touches C-03) | Added a minimal `@codehealth/db` re-export (`packages/db/index.ts`) exporting one shared `PrismaClient` singleton, since A-05 is the first task that actually persists anything | C-03 (the real client package task, owned by a teammate) wasn't done yet and A-05 can't upsert a `User` without a usable client. Kept it to the smallest possible slice — one singleton + re-exported types, nothing else — so it doesn't preempt the fuller repository-layer work C-03 still owns | Isn't touching packages/db stepping on a teammate's task? |
| 2026-07-23 | A-05 | Constructed `PrismaClient` with an explicit `@prisma/adapter-pg` driver adapter (`new PrismaClient({ adapter })`) instead of the bare constructor | Prisma 7's client engine ships with no bundled query-engine binary — `new PrismaClient()` throws `PrismaClientInitializationError` immediately unless a driver adapter is passed. This wasn't visible from the schema: Prisma 7 moved the connection URL out of `datasource db { url = ... }` and into `prisma.config.ts` (CLI-only), so the generated client needs the connection string handed to it again at runtime via the adapter | Why did the server crash on startup before any request even hit a route? |
| 2026-07-23 | A-06 | `requireAuth` middleware verifies the `Authorization: Bearer <jwt>` header with `jwt.verify` and attaches `{ id, username, platformRole }` to `req.user` via a global `Express.Request` type augmentation | Kept it fully stateless — no DB or Redis lookup per request — to match the "stateless JWT only" note already left in `jwt.ts`; Redis (A-09) isn't wired up until next sprint, so a session-store check wasn't an option yet anyway | Why doesn't the auth guard check the database on every request to confirm the user still exists? |
| 2026-07-23 | A-06 | `POST /auth/logout` just returns `204` with no server-side state change; `GET /auth/me` re-fetches the user row from the DB instead of trusting the JWT payload | A stateless JWT can't be truly revoked without a token denylist, which needs Redis — out of scope until A-09, so logout is client-side-only for now (client discards the token). `/auth/me` still needs the DB regardless, since `createdAt` isn't in the JWT payload, and it doubles as a check that catches a deleted user whose old token would otherwise still pass `requireAuth` | If logout doesn't invalidate anything server-side, what actually stops a token from being reused right after "logging out"? |
| 2026-07-23 | A-02 | Root `docker-compose.yml` for local Postgres 16 + Redis 7, credentials hardcoded as plain `environment:` values matching `packages/db/.env.example`'s documented default (`postgres`/`postgres`) | For local-dev-only infra, hardcoding throwaway credentials in a git-tracked compose file is standard practice (same pattern Postgres's own docs use) — the real deployed DB (Supabase/Railway) is configured separately through the platform's own env var UI, so there's no actual secret in this file to leak | Why is a hardcoded password in a committed file OK here but not in production? |
| 2026-07-24 | A-07 | `POST /webhooks/github` is mounted *before* the global `express.json()` in `app.ts`, using its own `express.raw({ type: 'application/json' })` so `req.body` stays the exact `Buffer` GitHub signed | If the JSON parser ran first it would consume the stream and hand the route an already-parsed object; re-serializing that back to a string before hashing isn't guaranteed to byte-match what GitHub originally sent (key order, whitespace), so the HMAC would fail unpredictably. Only the raw bytes are provably identical | Why not just parse the JSON first and verify the signature after? |
| 2026-07-24 | A-07 | `verifyWebhookSignature` compares the computed `sha256=<hmac>` to the `X-Hub-Signature-256` header with `crypto.timingSafeEqual`, not `===` | A plain string/`===` comparison exits on the first mismatched byte, so response time leaks how many leading bytes of a forged signature were already correct. `timingSafeEqual` always takes the same time regardless of where the mismatch is — this is the exact mitigation GitHub's own webhook security docs call for | Why does a webhook signature need constant-time comparison — isn't the secret already unguessable? |
| 2026-07-24 | A-07 | On a valid signature the handler currently returns a placeholder `202 { message: "Received" }` instead of api_design.md §3's full `{ message: "Job queued", jobId }` shape | A-07 in the WBS is scoped to receive + verify only — parsing the PR payload out of the body is A-08 and enqueueing the BullMQ job is A-10, both separate tasks with their own dependencies that don't exist yet. Returning the final shape now would mean fabricating a `jobId` | Why doesn't the response match the full api_design.md contract yet? |
| 2026-07-24 | A-08 | `webhookService.ts` parses the raw `Buffer` body into the exact extract shape from api_design.md §3 (`prNumber`, `headSha`, `githubRepoId`, …) and looks up the linked repo by `githubRepoId`; the route only does HTTP branching | Keeps the route thin (same pattern as `authService.ts`) and makes the parse/lookup pure enough to unit-test later without spinning up Express. `githubRepoId` is stringified because Prisma stores it as `String`, while GitHub sends a number | Why put parsing in a service instead of doing `JSON.parse` inside the route handler? |
| 2026-07-24 | A-08 | `ping` → `200 pong`; unsubscribed `pull_request` actions (e.g. `edited`) → `200` no-op; any other event type → `400 VALIDATION_ERROR` | api_design.md §3 says "400 unsupported event", but GitHub always fires a `ping` when the webhook is first registered (A-11) — rejecting it would mark the delivery failed and scare off evaluators during demo setup. Ignoring out-of-scope PR actions with 2xx also stops GitHub's retry storm for events we deliberately don't subscribe to | Why return 200 for `ping`/`edited` when the doc only lists 400 for unsupported events? |
| 2026-07-24 | A-08 | `findLinkedRepository` treats a missing row *or* `isActive === false` as "not linked" and throws `404 NOT_FOUND` | Unlinking (A-11) is expected to soft-delete via `isActive` rather than hard-delete the row (keeps historical snapshots). If we only checked existence, an unlinked repo would keep triggering analysis forever | Why check `isActive` instead of just `findUnique` and 404 on null? |
| 2026-07-24 | A-08 | Does **not** upsert a `PullRequest` row yet — only extracts + validates, then returns `202 { message: "Received" }` | A-08's WBS wording is "extract PR data, validate repo is linked"; persisting the PR and enqueueing the job belong to A-10 (and need BullMQ from A-09). Writing a half-persisted PR with no `AnalysisJob` would leave orphan rows for no gain | Why not save the PR to the database as soon as the webhook arrives? |
| 2026-07-27 | A-28 | `GET /health` in `apps/api` — pings Postgres with `prisma.$queryRaw`SELECT 1``, returns `200 healthy` / `503 degraded` with `checks: { database }` | Wrapped the probe in its own try/catch inside the handler instead of throwing into `AppError`/`errorHandler` — a dependency being down is the expected result this endpoint exists to report, not an unhandled error, so it needs to stay in the documented health-body shape rather than falling into the `{ error: ... }` envelope | Why doesn't a failed check throw like everywhere else in the app? |
| 2026-07-27 | A-28 | Left `checks.redis` and the worker's `/worker/health` out of this pass; API-side only for now | `apps/api` has no Redis client (that's A-09) and `apps/worker` is still just a `.gitkeep`, so implementing either would mean faking a check or scaffolding a worker ahead of schedule. `checks: { database }` today is honest about what's actually wired up; `redis` gets added once the BullMQ connection is real | Why doesn't the response match the full `checks` shape from api_design.md yet? |
| 2026-07-27 | A-29 | Shared Pino logger in `lib/logger.ts` — pretty-printed in dev (`pino-pretty` transport), raw JSON in prod, level from `LOG_LEVEL` — replacing the two `console.*` call sites (startup message, unexpected-error branch) | Matched the exact Pino config already written out in system_architecture.md §5.5.2 instead of improvising one; skipped `pino-http` since no doc names it as a dependency and A-29's scope is a logger, not per-request middleware | Why isn't every request auto-logged, if the architecture diagram lists "request logging" in the same middleware layer? |
| 2026-07-27 | A-29 | Health check's bare `catch {}` around the DB probe now logs `logger.warn({ err }, ...)` before setting `databaseOk = false`, instead of discarding the error | A silently swallowed error meant a real dependency failure showed up only as `"degraded"` with zero trail to debug it — the exact blind spot structured logging is supposed to close, so it was one line worth fixing alongside the rest of A-29 rather than leaving it | Isn't touching the health check outside A-29's task boundary? |
| 2026-07-28 | A-32 | `requireRepoAccess('read' \| 'write')` middleware: one `prisma.repository.findUnique` fetches the repo *and* the caller's own membership row (`members: { where: { userId } }`) in the same round trip, then branches on owner/admin/membership | Folds the existence check every repo route needs anyway together with database_design.md §3.7's Query 6 authorization check — same two lookups the doc specifies, one query instead of two, and it's index-only (PK + the `@@unique([userId, repoId])` B-tree) either way | Why fetch the membership row alongside the repo instead of a separate `can-access` query? |
| 2026-07-28 | A-32 | Write access (`POST`/`DELETE` on members) requires owner, `platformRole=ADMIN`, **or** an `ACTIVE` `TEAM_LEAD` member — not just owner/admin | api_design.md §2's general enforcement rule, database_design.md §3.7 ("the owner *or a TEAM_LEAD member* can grant scoped access"), and FR-5c/5d all agree `TEAM_LEAD` manages the repo like the owner; only the per-endpoint doc lines say "owner or admin" and §2 explicitly calls that shorthand for the general rule | The member-endpoint lines in the doc say "must be owner or admin" — why does a TEAM_LEAD get through? |
| 2026-07-28 | A-32 | `DELETE /api/repos/:repoId/members/:userId` sets `status: 'REMOVED', removedAt: new Date()` instead of deleting the row; a row already `REMOVED` reads as `404` | database_design.md §3.7 says `MemberStatus` + `removedAt` exist specifically "to support soft-removal and audit without deleting history" — a hard delete would throw that history away for no benefit | If the row still exists after DELETE, why does the API return 204 like it's gone? |
| 2026-07-28 | A-32 | `POST` on a user whose membership row exists but is `REMOVED` reactivates that row (`update` → `status: ACTIVE`, new `role`, `removedAt: null`) instead of inserting a second row or erroring | `@@unique([userId, repoId])` means a second insert would throw Prisma error P2002; reserving `409 CONFLICT` for a currently-`ACTIVE` member (the only case the doc actually calls a conflict) keeps a removed member re-addable rather than permanently stuck | Why does re-adding someone return 201 instead of 409 if a row for them already exists? |
| 2026-07-31 | C-09 | `Organization` mirrors a GitHub **account owner** (`repository.owner` — `{id, login, type}`), not just a GitHub org, with `OrgType` recording which kind it is | GitHub already treats personal accounts and orgs uniformly as repo owners, so copying that shape means personal repos need no special case — a user's own account is simply their own tenant. It also means `Repository.orgId` can be derived from the webhook payload, so the tenant is known on the one path that has no signed-in user | Where do personal repositories live if every repo has to belong to an organization? |
| 2026-07-31 | C-09 | `orgId` lives **only** on `Repository`, not denormalized onto `HealthSnapshot`/`Finding`/`AnalysisJob` — even though those already denormalize `repoId` | One tenant anchor means one place isolation can be wrong. A second copy could drift out of sync with its repository and become a silent leak, and the children already reach their tenant through `repoId` in one join. If Postgres row-level security is ever wanted, the column can be added then | `Finding` already carries a denormalized `repoId` for speed — why not denormalize `orgId` the same way? |
| 2026-07-31 | C-09 | Migration adds `orgId` as **nullable**, backfills it, then does `SET NOT NULL` — with hand-written `INSERT`/`UPDATE` between the Prisma-generated DDL | Prisma generates `ADD COLUMN "orgId" TEXT NOT NULL`, which fails on any table that already has rows, and it cannot infer how existing data maps onto a new tenant. Splitting the migration avoids a database reset, so teammates' local data survives. The backfill also moves existing repo members into the repo's new org — without that, adding the boundary would have silently revoked access they already had | Why is this the only migration with hand-written SQL in it, when the project rule says schema changes go through `prisma migrate dev`? |
| 2026-07-31 | A-33 | Cross-tenant requests return **404**, not 403; 403 is kept for "in the right tenant, no grant on this repo" | A 403 confirms the resource exists, and its existence is itself the other tenant's information — so the guard reports it as missing. Once the caller is inside the correct tenant they are entitled to know the repo is there, so the distinction still carries useful meaning | Isn't 403 the correct status for "you're authenticated but not allowed"? |
| 2026-07-31 | A-33 | Org membership is **not** put in the JWT; `requireRepoAccess` reads `OrganizationMember` from the database on every request | A 7-day token carrying `orgId` would keep a user inside a tenant for up to a week after they were removed from it, which defeats the point of the boundary. The cost is one indexed lookup, folded into the query the guard already runs to load the repo — so it is one extra index hit, not an extra round trip | Isn't checking the DB on every request exactly the stateless-JWT tradeoff A-06 deliberately avoided? |
| 2026-07-31 | A-33 | Deleted the `platformRole === 'ADMIN'` bypass from `requireRepoAccess`; `platformRole` now guards only operational routes | An admin who can read every organization is not tenant isolation, it is a documented hole in it. Since the requirement is that a user can *never* reach another org's data, an exception for our own operator accounts would make the claim untrue. Metrics and the queue dashboard expose counts and queue state, not anyone's code, so those stay admin-gated | We built the platform ADMIN role in C-08 — why remove the thing that made it powerful? |
| 2026-07-31 | A-33 | OAuth scope widened to `repo,user:email,read:org`, and `syncUserOrganizations` runs at the end of the login callback | Tenant membership has to come from somewhere authoritative, and GitHub already knows it — `read:org` is the smallest scope that reveals private org memberships too. Syncing at login means the boundary is populated before the user's first authenticated request, so there is no window where they are logged in but tenant-less | Why widen the OAuth scope — doesn't asking for more permissions make the consent screen scarier? |
| 2026-07-31 | A-33 | The sync also **revokes**: any `ACTIVE` membership GitHub no longer reports is set to `REMOVED` in the same transaction | A sync that only ever adds is not a sync, it is an accumulator — a user removed from a GitHub org would keep our access forever. Doing both directions in one transaction means the local picture is never a half-applied version of GitHub's | If membership only refreshes on login, how long can someone keep access after being removed on GitHub? |
| 2026-07-31 | A-33 | `GET /api/repos/:repoId/trend` was missing `requireRepoAccess('read')` — it had `requireAuth` only, despite a comment above it claiming the check. Added the guard | The guard was lost in the conflict resolution of merge `cd032b6`, where the trend endpoint and the member endpoints were written on parallel branches. Any logged-in user could read any repo's health, debt and vulnerability history. The comment describing the intended rule is what made it findable — a route whose comment and middleware chain disagree is worth grepping for elsewhere | How did an endpoint ship without its authorisation guard, and how would we catch the next one? |
| 2026-07-31 | A-33 | `addMember` now resolves the target user with `findFirst` filtered on the repo's org membership, and returns the **same** "no platform user found" message for an outsider as for a username that doesn't exist | Looking a user up globally would let a `TEAM_LEAD` pull someone from another tenant into this one, which defeats the boundary the rest of the task builds. Making the two failures indistinguishable also stops the endpoint being used to probe which usernames exist in other tenants | Why return "no such user" for a user that demonstrably does exist? |
| 2026-07-31 | C-09 | Seed creates **two** organizations, the second with its own user and repo, referenced by nothing else | A single-tenant fixture cannot demonstrate isolation — every test would pass trivially. The second tenant exists so "outsider requests tenant A's repo → 404" is runnable on a fresh clone, which is also the fastest way to show an evaluator that the boundary is real rather than asserted | Why seed a second organization that no other seed data points at? |
| 2026-08-10 | A-34 | Replaced the single 7-day JWT with a 15-minute access token plus an opaque refresh token stored as a SHA-256 hash in `Session` | The task said "Redis denylist", but the architecture document we submitted specifies `Session.tokenHash` with `validateSession()`/`revokeSession()` and claims session tokens are stored hashed — none of which the code did. A denylist would have closed the functional gap while leaving three submitted claims false, so I built the thing the document already described | The SAD says we issue a JWT. Why is there also a database row for every session? |
| 2026-08-10 | A-34 | Refresh tokens rotate: each use revokes the presented row and inserts a successor sharing a `familyId`. Replaying a spent token revokes the entire family | Rotation on its own buys little — the value is detection. A spent token being presented again means a copy exists, and since you cannot tell the thief from the victim, killing the whole family is the only safe response. `familyId` makes that one indexed `updateMany` instead of walking a chain of successor pointers | If you can't tell which of the two is the attacker, why is logging both out the right answer? |
| 2026-08-10 | A-34 | A 10-second grace window exempts a replayed token from reuse detection | Two browser tabs restoring at once both read the same cookie and both refresh; under strict rotation the loser presents a spent token and gets the user logged out for doing nothing wrong. A client-side mutex cannot fix this because the tabs are separate JS contexts sharing one cookie jar. Verified with two real browser contexts — both stayed signed in and the family survived | Doesn't a grace window mean a stolen token still works for ten seconds? |
| 2026-08-10 | A-34 | `requireAuth` still verifies signature only — no database read. Revocation is enforced at refresh time instead | Putting the session lookup on every request would make the tenant guard's indexed read the *second* database hit on the hot path. Revocation being effective within one access-token lifetime is a 15-minute bound, which is short enough to state plainly rather than engineer away | If the session table is the authority, why doesn't `requireAuth` check it? |
| 2026-08-10 | A-34 | The OAuth callback now completes the exchange server-side and 302-redirects instead of returning `{token, user}` to an XHR | An `HttpOnly` cookie set on a top-level navigation is the case `SameSite=Lax` always permits; set on a cross-origin XHR it is fragile and breaks outright if the web app and API are ever deployed on different sites. It also fixed React StrictMode's double-invoked effect colliding with the single-use nonce, which failed one of every two dev logins | Why did moving the exchange to the server delete a bug in the React client? |
| 2026-08-10 | A-34 | Access token held in a module-level variable in the web app, never `localStorage`; the refresh token is `HttpOnly` and unreachable from JS | A token in `localStorage` is readable by any XSS on the page, and it was the long-lived one. In-memory means an XSS can act while the tab is open but cannot walk away with a durable credential | If an attacker can run JS on your page anyway, what does moving the token out of `localStorage` actually buy? |
| 2026-08-10 | A-34 | Deleted the `demo-token` branch in `requireAuth` that mapped a literal string to a platform ADMIN identity | The web app wrote that token whenever no other was present, so every anonymous visitor was silently signed in as an admin and `ProtectedRoute` never redirected. It also swallowed genuine 401s, meaning "API is down" and "logged in" looked identical — which is why the app had no working logged-out state to remove it from until this task built one | What did removing eight lines of convenience code break, and why was that a good sign? |
| 2026-08-10 | A-34 | Added a `typ` claim to both token kinds and made `requireAuth` reject anything that isn't `typ: "access"` | `signState` and `signAppJwt` shared `JWT_SECRET` with no discriminator, so a 10-minute OAuth `state` token sent as a bearer token passed `jwt.verify` and reached Prisma with `id: undefined`. Same-secret tokens for different purposes need a type field or they are interchangeable | Two tokens signed with the same secret — what stops one being used as the other? |
| 2026-08-10 | A-34 | `requirePlatformRole` re-reads `platformRole` from the database instead of trusting the access token's claim | The token's role claim is up to 15 minutes stale by design. Everywhere else that staleness is an accepted tradeoff for skipping a DB read, but this middleware only guards admin routes, so one indexed `findUnique` is free and a demoted admin loses access on their very next request instead of at the end of their token's life | You said `requireAuth` doesn't touch the database — why does this one? |
| 2026-08-10 | A-34 | `setRefreshCookie`/`clearRefreshCookie` both build their flags from one private `cookieOptions()`, and the cookie uses `expires: expiresAt` rather than `maxAge` | A browser silently ignores a cookie deletion if the delete call's flags don't exactly match the flags it was set with, so set and clear sharing one function removes an entire class of "logout doesn't actually clear the cookie" bugs. `expires` pins the cookie to the session's absolute expiry; `maxAge` would reset a rolling window on every rotation and let a session outlive its intended 7 days | What would happen if the delete cookie call used slightly different options than the set call? |
| 2026-08-11 | A-35 | `/auth/github` now writes the state nonce to an `HttpOnly` cookie and the callback rejects any `state` whose nonce doesn't match it | Signing the state proved we issued it; it never proved *who* it was issued to, so any validly-signed state was accepted from any browser. An attacker could start a login, harvest their own state, and get a victim to complete it — leaving the victim signed into the attacker's GitHub identity and any repo they link going to the attacker's account. The cookie is the only part of the exchange the attacker can't supply | The state was already signed and single-use. What was still missing? |
| 2026-08-11 | A-35 | The single-use Redis nonce was kept rather than replaced by the cookie check | They stop different things. The cookie proves the flow started in this browser; the Redis check stops the same callback URL being replayed twice from *that* browser. GitHub's authorization code is single-use anyway, so the Redis half mostly saves a wasted round trip to GitHub — but it's already built and the submitted activity diagram draws it, so removing it would cost more than keeping it | If GitHub already rejects a reused code, why keep our own replay check? |
| 2026-08-11 | A-35 | Local `.env` still pointed `GITHUB_OAUTH_CALLBACK_URL` at the web app while `.env.example` documented the API. Fixed the live file | The rebuild moved the code exchange server-side, so GitHub has to redirect to the API. The example file was updated with the rest of the work and the real one was not, which meant GitHub login was quietly broken locally while every dev-login test kept passing — the two paths don't overlap. Worth remembering that a test suite green on one entry point says nothing about the other | Everything passed. How was the main login path broken at the same time? |
| 2026-08-10 | A-34 | Force-logout revokes every session for a user but doesn't touch their current access token | The access token is a signature-only JWT with no server-side record, so there is nothing to revoke — the admin action can only take away the ability to get a *new* one. The already-issued token keeps working for up to 15 more minutes, same bound as an ordinary logout | If an admin force-logs-out a user, why can that user still make requests for a few more minutes? |
| 2026-08-11 | A-10 | Wired the existing `enqueueAnalysisJob` (A-09, previously unused) into the webhook handler; added `upsertPullRequest` so `synchronize` updates the same `PullRequest` row instead of creating duplicates, keyed on the `@@unique([repoId, prNumber])` constraint | An upsert is required because GitHub fires `synchronize` on every push to the PR branch — a `create` would violate the unique constraint on the second push, and a naive "check then insert" has a race window `upsert` avoids | Why upsert instead of just inserting a new PullRequest row each time? |
| 2026-08-11 | A-10 | `closed` PR events update the `PullRequest` row to `MERGED`/`CLOSED` (using the payload's `merged` flag) and return `200` — no `AnalysisJob` is created | A closed PR won't receive further commits, so queuing an analysis for it burns a worker slot analysing code nobody will read. Only `opened`/`synchronize`/`reopened` still enqueue | Why does a `closed` webhook event return 200 with no job, while the others return 202? |
| 2026-08-11 | A-10 | `202` response is `{ message: "Job queued", analysisId, jobId }`, not the `{ message, jobId }` shape api_design.md §3 lists | api_design.md's own §6 (manual trigger) promises `analysisId` too, and `enqueueAnalysisJob` already returns both — `analysisId` is what a caller actually needs to poll job status, so the fuller §6 shape wins over the narrower §3 one | The webhook doc section only lists `jobId` in the response — why does the real response also include `analysisId`? |
| 2026-08-11 | A-10 | `cloneUrl` passed to `enqueueAnalysisJob` comes from the webhook payload (`event.cloneUrl`), not `repository.cloneUrl` | `Repository.cloneUrl` is nullable in the schema, so a linked repo could have it unset. The webhook payload always carries `clone_url` on the push that triggered this job, so it's the reliable source | Why read `cloneUrl` from the webhook payload when the Repository row already has a `cloneUrl` column? |
| 2026-08-11 | A-30 | Bull Board mounted at `/admin/queues`, gated by `express-basic-auth` on the whole `/admin` prefix; reuses the existing `analysisQueue` singleton from A-09 instead of opening a second Redis connection | api_design.md's `GET /admin/queues` spec calls for basic auth in env vars specifically instead of OAuth, so the dashboard stays reachable when login itself is broken. It's a completely separate credential from `PlatformRole.ADMIN` (checked via JWT + a DB read in `requirePlatformRole`) — same word, two unrelated auth mechanisms, one gating `/admin/queues` and the other `/api/admin/*` | Why does `/admin` use a totally different auth check than the `/api/admin` route right next to it? |
| 2026-08-11 | A-30 | Checked Helmet's default CSP (`script-src 'self'`) against Bull Board's actual served HTML before adding any override, instead of assuming a conflict | All of Bull Board's JS loads via same-origin `<script defer src="...">` tags, which `'self'` already permits; the one inline `<script>` tag is `type="application/json"` config data, not executable code CSP restricts. Confirmed live with curl against a running server rather than reasoning from the CSP spec alone | The plan flagged a likely CSP conflict — why does the final diff not touch helmet at all? |
| 2026-08-13 | A-18 | `POST /api/repos/:repoId/analyze` — `triggerManualAnalysis` in `queueService.ts` loads the repo, narrows to owner/org-admin, checks for an already-`PENDING`/`RUNNING` job, then calls the existing `enqueueAnalysisJob` unchanged | A-10 already anticipated this — `enqueueAnalysisJob`'s params comment says "both absent for a manual whole-repo analysis." So A-18 is a route plus a permission check narrower than `requireRepoAccess('write')` gives, not new plumbing | Why does the service re-check permissions when the route's middleware already ran one? |
| 2026-08-13 | A-18 | Manual trigger records `commitSha: 'HEAD'` instead of resolving a real SHA via GitHub | The worker already clones with `--branch=<branch>` and is the one component that actually knows the SHA; resolving it in the API would need the owner's decrypted GitHub token, which repos linked via the current `repoService.linkRepository` path don't reliably have, and a SHA fetched seconds earlier could be stale by clone time anyway | Why not call GitHub for the real commit SHA before queuing? |
| 2026-08-13 | A-18 | "Already in progress" returns `429 RATE_LIMITED`, checked via `AnalysisJob.status in (PENDING, RUNNING)` for the repo — no new index needed, `@@index([repoId, status])` already covers it | api_design.md's own error table for this endpoint asks for it, and it's the difference between one queued job and five if someone double-clicks the trigger button during the demo | Does that guard fully prevent duplicate jobs, or just make it unlikely? |
| 2026-08-14 | B-01 | `apps/worker`'s Redis connection is built with `maxRetriesPerRequest: null`, the opposite of the API's `3` | A BullMQ consumer parks on a blocking command waiting for the next job; BullMQ refuses to construct a `Worker` if the connection can give up on that wait after N retries. The API deliberately uses `3` because its Redis commands run inside an HTTP request that has to answer GitHub quickly — same library, opposite constraint, so the setting has to be opposite too | Why does the worker use a different Redis retry setting than the API when both files look almost identical? |
| 2026-08-14 | B-01 | The `worker.on('failed', ...)` listener's DB write is wrapped in try/catch instead of left bare | Found live: three jobs queued before a DB reseed had `analysisId`s that no longer existed, so the stub's Prisma update threw `P2025` as expected — but the `failed` listener is also `async` and runs outside the processor, so nothing else catches it. Its own Prisma write throwing was an unhandled rejection that killed the entire worker process over one bad job. Caught with a real orphaned-job test, not just code review | An `async` event listener threw and took the whole process down — why didn't the processor's own error handling catch that? |
| 2026-08-18 | 32a | Ran the full webhook path for real — ngrok tunnel, a live GitHub repo, PR opened/synchronize/closed, GitHub's own Redeliver button, three manual triggers back to back — instead of trusting a code read | Three consecutive manual triggers is the exact case B-01 was built to fix: before a consumer drained the queue, every job sat at `PENDING` and the second trigger always hit the 429 guard. Every piece of this path had only ever been tested in isolation; the row exists specifically to prove they work as one path | Why not just read `verifyWebhookSignature` and the worker code and call the integration done? |
| 2026-08-18 | 32a | Found `POST /api/repos` wired to `repoService.linkRepository`, a DB-only stub that never calls GitHub's `createWebhook` — the real implementation in `repoLinkService.ts` was fully built but imported by nothing | Linking the real test repo and getting back `webhookId: null` was the proof; reading the route file alone would have shown a plausible import, not that it was the wrong service. `git log` traced it to a "backend integration" commit that overwrote the correct wiring an earlier commit had | How did a fully-built, docs-conformant service end up dead code behind a broken stub with the same function name? |
| 2026-08-18 | 32a | Restored the route to `repoLinkService`, added a real `listAvailableRepos` (Octokit call, filtered to repos the caller has admin on *and* whose owner maps to an org they belong to here), deleted the mock and the duplicate `linkRepository` from `repoService`, and trimmed the web modal to post just `{ githubRepoId }` | api_design.md already specified this exact contract — body is only the id, the org is derived from the repo's GitHub owner — so the fix was restoring the documented shape, not inventing one. It also closed a real hole: the stub took `orgId` straight from the request body with no membership check, so any logged-in user could link a repo into an org they didn't belong to | Why does the fix remove more lines than it adds? |
| 2026-08-18 | 32a | GitHub delivered `synchronize` and `closed` 10–19 minutes late, indistinguishable at first glance from "never sent". Checked GitHub's status page afterwards: a Critical incident was open the entire session (created 13:40Z, webhooks listed as impacted) and the pass ran 13:54–14:20Z, fully inside it | First wrote this up as a property of GitHub webhooks and advised demoing off the manual trigger because of it — that was one bad afternoon generalised into a design claim. The tell I'd underweighted: a brand-new hook got no creation ping at all, but a forced ping on that same hook returned in ~1s, which is an outage signature, not a design characteristic. The results themselves still hold, since an outage produces false failures, not false passes | You tested during an outage — how do you know which of your findings are real? |
| 2026-08-18 | 32a | Kept the "out-of-order delivery corrupts PR status" finding but downgraded how likely it is, and dropped the demo advice that came from the lag | The observation was manufactured by the outage — a late `synchronize` landing after a `closed` — but the weakness survives the caveat on its own merits, because GitHub guarantees no ordering between deliveries and ordinary retries reorder them on a healthy day too. `upsertPullRequest` writes `status` with no timestamp comparison either way. Separating "what I saw" from "why it's a bug" is what let one finding stay and the other be retracted | If the outage caused what you observed, why is this still a bug? |
| 2026-08-18 | B-02 | `cloneRepository` uses `mkdtemp` for a per-job workspace instead of a fixed or repo-keyed path | `WORKER_CONCURRENCY=2` means two clones can be live in the same process at once; a shared or predictable path would let one job's clone stomp another's mid-write | Why not just clone into a directory named after the repo? |
| 2026-08-18 | B-02 | The GitHub token is injected into the clone URL as `x-access-token:<token>@github.com/...`, and every git error is redacted (`token → ***`) before it's logged or written to `AnalysisJob.errorMessage` | git repeats the remote URL verbatim in its own error text, so a failed clone would otherwise put a live token into a pino log line and into a database column, both places already known to be read by more people than a stack trace should reach | If the token is only ever used locally to clone, how could it end up in the database? |
| 2026-08-18 | B-02 | Clone runs with `GIT_TERMINAL_PROMPT=0` | Without it, a missing or rejected token doesn't fail fast — git blocks on an interactive credential prompt nobody is there to answer, holding the job open past its retries into the 5-minute job timeout instead of failing in the few seconds a real auth error takes | What actually breaks if that env var is left out — the clone still fails either way, doesn't it? |
| 2026-08-18 | B-02 | After the shallow clone, `git rev-parse HEAD` resolves the real commit and overwrites `AnalysisJob.commitSha`, which A-18 could only record as the placeholder `'HEAD'`/`'manual'` for a manual trigger | The worker is the one component that actually has the repo checked out, so it's the only place that can know the true SHA without a second GitHub call; row 66 already flagged this gap when A-18 shipped it | Why wasn't the real SHA resolved back when the job was first queued? |
| 2026-08-18 | B-02 | `decrypt()` was copied into a new `apps/worker/src/lib/crypto.ts` rather than moving the API's version into `packages/shared` | The worker only ever reads tokens the API already encrypted, never writes them, so it needs half the function surface; duplicating ~20 lines was smaller than restructuring a shared package and touching `apps/api` for a task scoped to the worker | Isn't a second copy of the same crypto function a maintenance risk? |
| 2026-08-18 | B-02 | Wrapped the clone in `try/finally` and added the `rm -rf` cleanup now, even though it's formally B-23's task | With `attempts: 3` on the queue, a clone that keeps failing would otherwise leave up to three orphaned temp directories in `/tmp` per job before B-23 ever lands; B-23 becomes hardening this path, not building it from nothing | Why does a task scoped to cloning also include the cleanup step? |
