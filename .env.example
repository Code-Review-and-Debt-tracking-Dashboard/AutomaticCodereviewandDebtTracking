# Read by docker-compose.prod.yml. Copy to .env on the server.

# app.<domain> serves the web app, api.<domain> the API
DOMAIN=example.com

# hex only, it goes straight into the DATABASE_URL
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
POSTGRES_PASSWORD=
