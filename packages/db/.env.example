# Standard PostgreSQL connection string (localhost:5432).
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
#
# Copy this file to .env and update the credentials to match your local setup.
# Common defaults: user=postgres, password=postgres, db=postgres
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5433/code_review_db"
